<script server>
import { defineProps, withDefaults } from 'stx'

interface ProviderChipProps {
  /** Iconify simple-icons slug, e.g. "bluesky". */
  icon: string
  name: string
}

const { icon, name } = withDefaults(defineProps<ProviderChipProps>(), {})
</script>

<template>
  <li class="inline-flex gap-2.5 items-center text-ink">
    <span class="h-5 w-5 i-simple-icons-{{ icon }}"></span>
    <span class="folio">{{ name }}</span>
  </li>
</template>
