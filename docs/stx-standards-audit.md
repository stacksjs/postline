# stx standards audit — Postline

Generated from a 13-agent audit of this repo against `~/Documents/stx-standards`
(the twelve chapters, plus the two RFC drafts), run against the installed
`@stacksjs/stx@0.2.119`.

**238 findings — 20 critical, 61 high, 86 medium, 71 low.**

Stage 0 (the four build-breaking criticals) is **done** — see commit
`db04bda`. Everything below is what remains. Findings are verbatim from the
audit; each was required to carry a `file:line`. They have **not** all been
independently re-verified — the Stage 0 set was, and every claim in it held.

Two defects found during this audit were filed upstream:
[stacksjs/stx#1793](https://github.com/stacksjs/stx/issues/1793) (storage
composables: unguarded `JSON.parse` and `setItem`, 6 sites) and
[#1794](https://github.com/stacksjs/stx/issues/1794) (`useColorMode` has no
pre-paint story). Both join [#1788](https://github.com/stacksjs/stx/issues/1788).

## One correction to the standards

Rule 11 of the twelve says `client` on a script block is *redundant*. Against
0.2.119 it is not. `dist/script-classifier.js:12` falls a bare `<script>`
through to `type: "server"` unless its body calls `state(` / `derived(` /
`effect(` (the `SIGNALS_PATTERN` test at `:1`, `:8`). So a bare `<script>`
holding imperative DOM code classifies as **server**. Always write
`<script client>` explicitly. The RFC's own P11b flags the same inconsistency.

---

## Blocked on upstream

Two conversions are deliberately not done, each for a verified framework
reason rather than a lack of appetite:

**The marketing layout keeps its `<!DOCTYPE>`.** Its design tokens are scoped
to a class on the root element, and `generateDocumentShell` has no `htmlAttrs`
support — the option is typed at `head.d.ts:193` and computed at
`process.js:279`, then never read, while its sibling `bodyAttrs` *is* emitted.
Removing the shell today silently strips the landing page's palette, base font
and mega-menu CSS. Filed as [stacksjs/stx#1798](https://github.com/stacksjs/stx/issues/1798)
(fixed upstream, awaiting a release). When it ships, drop the exemption from
`DOCTYPE_EXEMPT` in `scripts/stx-gate.ts`.

**The composer's textareas stay imperative.** `bindModel`'s text branch is
`effect(() => { el.value = getValue() ?? '' })` with no check that the value
changed, and the runtime contains zero `selectionStart`/`setSelectionRange`.
Every keystroke therefore re-assigns `.value`, which sends the caret to the end.
It is invisible while appending — which is why `x-model` on the login inputs
feels fine — and breaks editing mid-text. The code being replaced deliberately
never writes back to the textarea while typing. Adopting `x-model` there would
be a regression. Filed as [stacksjs/stx#1799](https://github.com/stacksjs/stx/issues/1799).

This also constrains the remaining nine `document.createElement` calls in
`composer.stx`: seven build the per-network override rows and one builds a post
article, all of which own a textarea. They convert cleanly once #1799 lands.

## Reading the paths below

Findings were recorded **before** the Stage 0 moves, so they cite the old
locations. Translate as you go:

| Cited as | Now at |
|---|---|
| `resources/views/layouts/*.stx` | `resources/layouts/*.stx` |
| `components/PostlineSidebar.stx` | `resources/components/PostlineSidebar.stx` |
| `resources/views/partials/theme.stx` | `resources/partials/theme.stx` |
| `resources/views/components/FeatureCard.stx` | `resources/components/FeatureCard.stx` |
| `resources/views/marketing/README.md` | `docs/marketing/README.md` |

Findings already closed by Stage 0 — ignore them here: everything under rules
**1.1**, **1.2**, **1.4**, **3.3**, **3.12**, **4.8**, **10.12**, **12.1**, and
the `strict`-key half of **8.1** and **9.13**.

## Remaining findings by severity

### critical (20)

- **config/ui.ts:17** — [8.1] *(silent)* The entire default export is three directory keys and nothing else — no `strict` key. stx's client-script DOM validator therefore runs on every non-server <script> body and discards every finding. The burn-down script from the chapter exits 1 with `violating blocks: 8` today.
  - **Fix:** Add `strict: { enabled: true, failOnViolation: process.env.NODE_ENV !== 'production', allowPatterns: [] }` to the default export in config/ui.ts. Note the chapter's WRONG/RIGHT snippet assumes a `router:` key sits next to it; Postline has no router key in config/ui.ts (it sets window.STX*ROUTER*OPTIONS inline at resources/views/layouts/postline.stx:62), so add `strict` next to the three dir keys. Turn it on warning-first, then flip failOnViolation.
- **resources/views/index.stx:118** — [8.3] *(silent)* A single plain `<script>` block spanning lines 118-1334 (1,216 lines) implements the entire composer imperatively. It opens by caching 30+ `root.querySelector(...)` handles (lines 120-165) in a closure. Because the shell swaps `[data-postline-workspace]` on SPA navigation, every one of those handles points at a detached node after the first in-app navigation.
  - **Fix:** Convert to `<script client>`, replace each `root.querySelector('[data-x]')` with a `ref="x"` attribute on the markup plus `const x = useRef('x')`, and replace the data-attribute state machine (activeMode, canPublish, providers, selected, editingId) with `state()` signals bound through `:text`, `:class` and `:if`. $refs survives the swap (signals.js:5582-5585); a closure-cached selector does not.
- **resources/views/layouts/postline.stx:293** — [8.4] *(silent)* The app shell's behaviour script (lines 293-508) runs on every page and is entirely imperative: 16 querySelector/querySelectorAll calls (296-300, 318, 321, 341, 350, 358, 363, 377-378, 389, 408, 422), 14 raw addEventListener registrations (344, 353, 361, 366, 449, 459, 471-473, 502-506), and a hand-rolled `button.dataset.bound === 'true'` re-entry guard at 342-343/351-352/359-360/364-365 whose only purpose is to undo the duplicate-handler stacking that useEventListener's onDestroy would have prevented.
  - **Fix:** Convert the block to `<script client>` and replace each `element.addEventListener(...)` with a `@click` directive on the markup (sidebar collapse, expand FAB, back/forward) or `useEventListener(event, handler, { target })` for the window/document-level ones. The `dataset.bound` guards then delete themselves — useEventListener's onDestroy (signals.js:4053) is flushed on stx:load, so nothing stacks.
- **resources/views/layouts/postline.stx:302** — [8.5] *(silent)* syncSidebarCollapse (295-335) sets visual state by hand across 20 lines: `style.setProperty` at 302-303, `classList.toggle` at 304/307-310/319/322/330, direct `.style.width/.minWidth/.overflow` at 313-315, and `setAttribute` at 311/325-326/331. syncNativeShell (376-419) writes 15 more inline styles at 384-385, 391, 393-398, 403-404, 410-418. All of it runs once per call and is erased by the next workspace swap.
  - **Fix:** Hoist `collapsed` and `native` into signals in a `<script client>` block, then bind: `:class="{ 'px-5 pt-[68px] pb-5': !collapsed() }"` on the sidebar, `:aria-hidden="collapsed()"`, `:if="!collapsed()"` on the `[data-sidebar-expanded]` and `[data-sidebar-collapsible-content]` nodes, and move the `--postline-sidebar-width` swap into a CSS rule keyed off the existing `html.postline-sidebar-collapsed` class rather than three separate setProperty calls.
- **resources/views/index.stx:235** — [8.6] *(silent)* DOM is constructed in JavaScript at 15 sites: `document.createElement` at 235, 297, 301, 304, 308, 319, 330, 337, 345 and 530, and `.innerHTML =` with an interpolated Crosswind class string at 245, 252, 534, 665 and 676. Line 245 and 252 hand-write the full utility-class list for a provider chip; the 100-char class string at 225-229 (`toggleClass`) is the page's own design tokens re-typed in JS.
  - **Fix:** Replace createProviderToggle/renderProviderToggles (232-256) with `@foreach (providers() as p)` over a `providers` signal plus `:class="{ ... }"` and `:aria-pressed`; replace createPost/postTemplate (500-536) with `@foreach` over a `posts` signal; replace renderLinkPreview's innerHTML (659-684) with markup guarded by `:if="preview()"` and `:text` bindings. The `escapeHtml` helper and the `hidden`/`inline-flex` class juggling both disappear.
- **resources/views/layouts/postline.stx:1** — [2.2] *(silent)* The primary app layout is a full HTML document: `<!DOCTYPE html>` (line 1), `<html lang="en" class="text-ink bg-bg">` (2), `<head>` (3), `</head>` (262), `<body class=…>` (263), `</body></html>` (510-511). Six of the eight active pages extend it (index, queue, timeline, settings, accounts, analytics). Because §2.2's test runs on the COMPOSED output, `hasDocumentShell(result)` is true for every one of them (verified: node_modules/@stacksjs/stx/dist/process.js:275), so `ensureDocumentShell` returns the HTML untouched (document-shell.js:101-104) and the framework's head-assembly path at process.js:258-274 is bypassed; only `injectConfigHeadTags(result, baseHeadConfig)` runs, which never carries `title` (document-shell.js:71). Confirmed empirically by rendering a doctype-layout page through the installed 0.2.119: the shell was NOT synthesised and the layout's own `<title>` rendered first, ahead of the injected runtime title. No warning, no log line.
  - **Fix:** Delete lines 1-3, 262-263 and 510-511. Move `<link rel="icon">` (9), the two `<link rel="stylesheet">` (260-261) and the pre-paint `<script>` blocks (10-62) into a `@head … @endhead` block, and add `@stack('styles')` inside it. Move the `class="text-ink bg-bg"` / body classes to `bodyClass`/`htmlAttrs`/`bodyAttrs` in the app head config, which `generateDocumentShell` reads at document-shell.js:18-19, 46-47. Delete `<title>@yield('title')</title>` (8) and `<meta name="description" content="@yield('description')">` (7) rather than porting them (see 2.6).
- **config/ui.ts:8** — [1.1] *(silent)* config/ui.ts declares no `root` and no `pagesDir`, so resolveStxRoot (node_modules/@stacksjs/stx/dist/config.js:363-374) infers them from fs.existsSync. Verified by calling loadStxConfig() against this repo: root resolves to 'resources' and pagesDir to 'views'. The sole reason root is not '.' is that resources/layouts/ exists — and its only file is resources/layouts/Desktop.stx, a leftover desktop-shell demo. Deleting that one file silently changes the resolved root, the three template directories, the SSG entry pages dir, the store directory, the composable-loader probe root, and the component convention fallback base.
  - **Fix:** Add `root: '.'` and `pagesDir: 'resources/views'` as the first two keys of the exported object in config/ui.ts. Note the 0.2.119-specific interaction: once root is '.', composable-loader's probeDefaultDirs resolves ['composables','functions'] against the project root, so resources/functions stops being discovered — add `composablesDir: 'resources/functions'` in the same edit (or move those files into resources/stores and declare `storesDir: 'resources/stores'`).
- **config/ui.ts:10** — [1.2] *(silent)* componentsDir/layoutsDir/partialsDir are written project-root-relative but root is inferred as 'resources', so config.js:409-418 prefixes all three. Verified by running both loaders: Loader B (serve) sees 'resources/components' / 'resources/layouts' / 'resources/partials'; Loader A (build/SSG/production-builder/store-loader/Crosswind discovery) sees 'resources/resources/components' / 'resources/resources/layouts' / 'resources/resources/partials', all with exists=false. Three of the three keys the file sets resolve to directories that do not exist on the build path. Lines 13 and 16 carry the same defect.
  - **Fix:** Keep the three values exactly as they are — they become correct the moment `root: '.'` is pinned per 1.1, because that makes the config.js:409 `if (loaded.root !== '.')` branch false and both loaders agree. Do not instead set root: 'resources' and shorten the values; that makes Loader B miss.
- **config/ui.ts:8** — [4.8] *(silent)* config/ui.ts is 17 lines and sets neither `skipDefaultSeoTags: true` nor an `app.head` block. Because `layouts/postline.stx` emits no og:title/twitter:title and no page sets a runtime head, `injectSeoTags` (seo.js:243-308, run from process.js:678) satisfies none of its early-return conditions and splices placeholder tags in right after `<head>`. Verified with the real pipeline and in dist/: index, queue, timeline, settings, accounts and analytics all ship `<meta name="title" content="stx Project">`, `og:title=stx Project`, `og:description=A website built with stx templating engine`, `twitter:title=stx Project`, `twitter:description=A website built with stx templating engine` — plus a SECOND `<meta name="description">` carrying the placeholder that is injected BEFORE the real one from layouts/postline.stx:7, so the first description a crawler reads on every app page is "A website built with stx templating engine". Nothing warns.
  - **Fix:** Add `skipDefaultSeoTags: true` to the config/ui.ts default export, plus an `app: { head: { title: 'Postline', meta: [{ charset: 'utf-8' }, { name: 'viewport', content: 'width=device-width, initial-scale=1' }], link: [{ rel: 'icon', type: 'image/svg+xml', href: '/favicon.svg' }] } }` block. `injectConfigHeadTags` (document-shell.js:92+) dedups by meta name/property/charset and by exact link href, so this is safe to land before touching either layout.
- **config/ui.ts:10** — [3.12] *(silent)* componentsDir is set to 'resources/components', but dist/config.js:409-418 prefixes loaded.root ('resources', from resolveStxRoot at dist/config.js:367-369) onto it again, producing 'resources/resources/components'. Combined with PostlineSidebar.stx living at the project-root components/ directory — which is in NO search path — every production-built app page ships a rendered error string in place of the sidebar. Verified in dist/: index.html, queue.html, settings.html, timeline.html, analytics.html and accounts.html each contain '[Error loading component: ENOENT: no such file or directory, open \'postline-sidebar\']' followed by a dump of 12 absolute filesystem paths starting '/Users/glennmichaeltorregosa/Documents/Projects/postline/resources/resources/components/…'. This is the identical failure the chapter documents at bughq's dist/layouts/marketing.html.
  - **Fix:** Make the three directory keys root-relative and move the component into a searched directory: `componentsDir: 'components'` (→ resources/components) in config/ui.ts, and move components/PostlineSidebar.stx to resources/components/PostlineSidebar.stx. Also add the explicit manifest the chapter prescribes: root: 'resources', pagesDir: 'views', layoutsDir: 'layouts', partialsDir: 'partials', defaultLayout: 'default'.
- **config/ui.ts:13** — [3.12] *(silent)* layoutsDir is 'resources/layouts' and becomes 'resources/resources/layouts' after the root join at dist/config.js:409-418. It survives today only because @stacksjs/actions/dist/dev/views.js:59-62 overrides it entirely via firstExistingPath(['resources/views/layouts','resources/layouts']) → 'resources/views/layouts', and bun-plugin-stx/dist/serve.js:8936 gives the passed option priority. The config file is therefore load-bearing-looking but inert in dev and wrong everywhere else.
  - **Fix:** Set `layoutsDir: 'layouts'` and move the layouts to resources/layouts/ (rule 3.3), so the value and the filesystem agree without the dev-server override.
- **resources/views/layouts/postline.stx:1** — [3.3] *(silent)* The primary app layout lives inside pagesDir. resolveStxRoot (dist/config.js:367-369) makes everything under resources/views a route, and SSGConfig (dist/ssg.d.ts:24-43) has no excludeDirs key, so the static build emits it as a page. Verified in the checked-in build output: dist/layouts/postline.html exists (213,983 bytes) and dist/sitemap.xml contains <loc>http://localhost/layouts/postline</loc>. That published page body contains the absolute-filesystem-path ENOENT dump described above.
  - **Fix:** git mv resources/views/layouts/postline.stx resources/layouts/postline.stx. Layout resolution is unaffected: resolveTemplatePathInner (dist/utils.js:769-805) walks up to 10 parents for a directory literally named `layouts` and dist/utils.js:787 strips a leading 'layouts/' from the argument, so @extends('layouts/postline') still resolves and deriveLayoutGroup still yields 'postline'.
- **resources/views/settings.stx:138** — [6.3] *(silent)* `const authToken = useCookie('auth-token')` declares the session cookie with NO options, while login.stx:105-106 declares the same cookie with `{ maxAge }` (30 days by default). Verified in 0.2.119: useCookie calls effect(…) (dist/signals.js:3946) and effect runs immediately (dist/signals.js:624 `if (options.immediate !== false) runEffect()`), so merely LOADING /settings re-serialises the cookie as `auth-token=<token>; path=/; SameSite=Lax` with no max-age — silently downgrading a 30-day persistent cookie to a session cookie. Closing the browser after visiting Settings logs the user out. This is precisely the failure 6.3 exists to prevent: one fact, two declarations, divergent policy.
  - **Fix:** Declare the cookie once in `resources/stores/session.ts`: `defineStore('session', () => { const token = useCookie('auth-token', { maxAge: 2592000 }); return { token, signOut() { token.set(''); location.assign('/login') } } })`. settings.stx then does `const session = useStore('session')` inside onMount and calls `session.signOut()`; login.stx calls `session.token.set(token)`. One maxAge, one place.
- **resources/views/accounts.stx:183** — [6.6] accounts.stx runs 7 raw fetch() calls (:183, :261, :341, :361, :458, :554, :625, :641) inside a 562-line vanilla IIFE with hand-written per-provider loading/error/pill DOM updates and no shared cache. Zero useQuery/useMutation. Navigating /accounts ⇄ /settings ⇄ / refetches every provider status from scratch every time, because nothing survives the fragment swap.
  - **Fix:** Convert accounts.stx to a `<script client>` and replace each status read with `useQuery('/api/postline/bluesky/status', { cacheKey: 'bluesky-status', staleTime: 30000 })`, binding `q.data()`, `q.loading()`, `q.error()` in the template; replace each connect POST with `useMutation('/api/postline/<provider>/connect', { onSuccess: () => q.invalidate() })`. Move the shared provider status into `resources/stores/providers.ts` so /settings and / read the same request.
- **resources/views/index.stx:118** — [6.1] *(silent)* The composer — Postline's primary page, 1218 lines of client script — opens with a bare `<script>` (not `<script client>`) and contains zero state(), zero derived(), zero stx directives (verified: grep for x-show/x-for/x-if/:attr/@click over index.stx returns nothing). Every computed value (character count, publish label, provider enablement, thread post numbering) is a plain function that must be manually re-invoked after each mutation — updateCount(), updatePublishLabel(), renamePosts(). Miss one call site and the UI silently desynchronises.
  - **Fix:** Change the tag to `<script client>` and lift the composer's facts into signals: `const posts = state([]) / const activeIndex = state(0)`, then `const activeText = derived(() => posts()[activeIndex()]?.text || '')`, `const charCount = derived(() => activeText().length)`, `const publishLabel = derived(() => scheduleAt() ? 'Schedule' : 'Publish now')`. Bind with `:text="charCount()"` and delete the manual update* call chain.
- **resources/views/layouts/postline.stx:265** — [10.12] *(silent)* `<PostlineSidebar />` never resolves in a production build. The component file lives at `components/PostlineSidebar.stx` (project root). Because config/ui.ts sets no `root`, config.js:363-374 resolves `root: "resources"` (both resources/views and resources/layouts exist), so utils.js:426-433 searches `resources/resources/views/components`, `resources/resources/components`, `resources/src/components` and `resources/components` — never the project-root `components/`. Every built page therefore ships the literal string `[Error loading component: ENOENT: no such file or directory, open 'postline-sidebar'] Searched paths: /Users/glennmichaeltorregosa/Documents/Projects/postline/...` — leaking the developer's absolute home directory to every visitor — and loses ALL navigation: dist/index.html contains zero `data-testid="postline-nav"`, zero `<aside data-postline-sidebar>`, and zero nav anchors. Confirmed in 7 built files: dist/{index,queue,timeline,settings,accounts,analytics}.html and dist/layouts/postline.html. The build exits 0. (Dev-server behaviour not verified — bun-plugin-stx/dist/serve.js:8865 defaults componentsDir to "components" relative to cwd, which likely masks this in dev, making it a production-only failure.)
  - **Fix:** `git mv components/PostlineSidebar.stx resources/components/PostlineSidebar.stx` — that path IS in the resolver's fallback list (`path.resolve(configuredRoot, "components")` = `<root>/resources/components`), and it is the location §10.12 mandates. Then add a CI gate: `grep -rn "Error loading component" dist/ && exit 1`.
- **config/ui.ts:8** — [12.1] *(silent)* The entire default export is three keys — componentsDir, layoutsDir, partialsDir. There is no `strict` key, so `loadStxConfig()` returns strict: undefined, bun-plugin-stx's `"strict" in stxConfig` guard (dist/serve.js:9546, :9721) never forwards it, dist/script-validation.js:129 falls back to `{ enabled: false }`, and the guard at :148 throws away every finding the 25-pattern table produces. Consequence measured on Postline: 82 prohibited DOM calls across 5 .stx files render on every request with no warning, in dev or production. The file is also missing `plugins` and `storesDir`, which 12.9 step 3 and 12.10 item 3 both require.
  - **Fix:** Add to the default export: `strict: { enabled: true, failOnViolation: false, allowPatterns: ['location.replace()'] }` — allowPatterns is matched against the rule's `message` or its `pattern.source` (dist/script-validation.js:131), and `'location.replace()'` suppresses exactly the pre-paint auth guard at layouts/postline.stx:17. Keep failOnViolation false until the 82 are cleared, then flip it so regressions throw at render. Add `plugins` and `storesDir` in the same commit.
- **tsconfig.json:16** — [11.9] *(silent)* `include` is narrowed to *.ts, *.d.ts, app/Actions/**, app/Models/**, app/Services/**, app/Support/** — and `exclude` (line 30) additionally drops resources/assets/**. Measured with `tsc --noEmit -p tsconfig.json --listFilesOnly`: 68 of 138 project .ts files are inspected. Unchecked: all 41 config/*.ts, all 4 routes/*.ts, 8 files / 3,103 lines of browser-shipped resources/assets/scripts/*.ts, app/Jobs (4), app/Middleware, app/Mail, app/Listeners, app/Commands, resources/functions (2). `bun run typecheck` exits 0 today because it barely looks at anything. Note the root globs *.ts and *.d.ts match zero files — there are no root .ts files.
  - **Fix:** Replace `include` with ["*.ts", "*.d.ts", "types/**/*.d.ts", "app/**/*.ts", "config/**/*.ts", "resources/**/*.ts", "routes/**/*.ts", "components/**/*.ts"] and drop "resources/assets/**" from `exclude`. Measured cost: running tsc against storage/framework/tsconfig.app.json (which already has the wide include, minus resources/assets) produces exactly 5 errors — config/commit.ts:2 (two non-existent imports from @stacksjs/utils), config/deps.ts:18 and :19 (invalid version literals), config/query-builder.ts:67 (missing snapshotDir). All real, all cheap.
- **config/crosswind.ts:21** — [11.2] *(silent)* theme.extend.colors maps 11 semantic names to var(--bg), var(--panel), var(--panel-2), var(--panel-3), var(--line), var(--ink), var(--body), var(--muted), var(--accent), var(--on-accent) — but the config declares NONE of those variables. There is no `preflights` key at all. Every one of those utilities depends on a :root block hand-written inside a .stx <style> tag, and there are two mutually inconsistent copies of it.
  - **Fix:** Add `preflights: [{ getCSS: () => ':root{' + LIGHT + '}@media (prefers-color-scheme: dark){:root{' + DARK + '}}html.dark{' + DARK + '}:root[data-theme="light"]{' + LIGHT + '}' }]` with LIGHT/DARK as module consts, and delete the :root/html.dark blocks from both layouts. Verified in @cwcss/crosswind 0.2.12: `preflights` is a real key (types.d.ts:145, Preflight at :136-138), the generator honours it, and it survives the render-path merge at dev-server/crosswind.js:304-313 because userConfig is spread wholesale.
- **resources/views/layouts/postline.stx:63** — [11.1] 197 lines of hand-written CSS (lines 63-259) in the app layout that every one of the six active pages extends. Contents: the :root/html.dark token blocks (74-102), a 60-line `.dark .utility {}` override layer re-declaring generated utilities by hand (114-179), an @property/@keyframes-adjacent sidebar transition system (181-238), and a media query (240-253). None of it is visible to the Crosswind extractor, none of it can be purged or deduped.
  - **Fix:** Tokens → config/crosswind.ts `preflights` (rule 11.2). The `.dark .bg-*`/`.dark .text-*`/`.dark .hover\:*` override layer (114-179) is entirely redundant once status colours become var-backed tokens (e.g. add `success`, `warning`, `danger`, `info` surface/border/text tokens to theme.extend.colors and use bg-success-surface etc.). The sidebar transition block and the @property declaration are genuine unreachable-by-utility CSS → move to `preflights` so they land inside the one style tag the SPA router preserves.

### high (61)

- **resources/views/index.stx:1161** — [8.4] *(silent)* 21 raw addEventListener registrations at 1161, 1168, 1176, 1183, 1198, 1199, 1200, 1215, 1220, 1221, 1226, 1240, 1251, 1255, 1276, 1290, 1294, 1295, plus `window.addEventListener('beforeunload', saveDraft)` at 1300 and `document.addEventListener('DOMContentLoaded', ...)` at 1328. None is ever removed, and `grep -rn 'onDestroy' resources/ --include='*.stx'` returns 0 across all 31 files.
  - **Fix:** Move each handler onto its element as `@click` / `@input` / `@change` (dispatched at signals.js:2073+ with modifier parsing), and use `useEventListener('beforeunload', saveDraft)` for the window-level one. Delete the DOMContentLoaded bootstrap at 1327-1332 in favour of `onMount(start)` — onMount is auto-imported and fires per hydration, which DOMContentLoaded does not.
- **resources/views/accounts.stx:288** — [8.7] *(silent)* Raw history manipulation to scrub an OAuth callback query param, duplicated verbatim four times: `window.history.replaceState({}, '', `${window.location.pathname}${query ? `?${query}` : ''}`)` at lines 288, 387, 483 and 579, each preceded by a manual `new URLSearchParams(window.location.search)` read at 276, 375 and equivalents.
  - **Fix:** One `const params = useSearchParams()` at the top of the block (auto-imported; verified at signals.js:1024). Read with `params.get('linkedin')`, then `params.setAll({ linkedin: undefined, message: undefined })`, which rewrites the URL and resyncs its own signal. That collapses all four copies into one shared helper and survives SPA navigation, which replaceState-on-load does not.
- **resources/views/accounts.stx:118** — [8.3] *(silent)* The whole page is a 560-line plain `<script>` (116-676) built on a cached `document.querySelector('[data-testid="accounts-page"]')` root at 118 and ~30 descendant selector handles, then rendered by assigning `.textContent` and `.className` at 142, 144, 151-158, 160, 161, 212, 224-231, 234 and the same block repeated for Twitter, Instagram, Threads and Mastodon. `status.className = ...` at 152-158 rewrites a full Crosswind class string in JS.
  - **Fix:** Convert to `<script client>`, hold one `channels` signal array, and render the four provider cards with `@foreach` + `:text` + `:class="{ 'border-emerald-200 bg-emerald-50 text-emerald-700': connected, ... }"`. The five near-identical renderX/refreshX pairs collapse into one.
- **resources/views/index.stx:593** — [8.8] *(silent)* Two hand-rolled debouncers using bare timers with module-scoped handles: `clearTimeout(draftSaveTimer)` / `draftSaveTimer = setTimeout(saveDraft, 100)` at 592-593, and `clearTimeout(previewTimer)` / `previewTimer = setTimeout(...)` at 718-719. Neither is registered with any destroy hook, so a pending timer fires against detached nodes after the workspace swap.
  - **Fix:** `const saveDraftDebounced = useDebounce(saveDraft, 100)` and `const previewDebounced = useDebounce(() => updateLinkPreview(activePost), 250)` — both auto-imported (STX*AUTO*IMPORTS at client-script.js:143), both self-cancelling. If you prefer explicit timers, `useTimeout` starts on creation and registers onDestroy(stop) at signals.js:3627-3628, so create it, call `.stop()` once to park it, then `.start()` per keystroke.
- **resources/views/layouts/postline.stx:286** — [8.12] *(silent)* Seven `<script type="module" src="/assets/scripts/postline-*.ts">` tags (286-292). skipAttrs at process.js:758 skips any tag with `src=` or a non-JS `type=`, and every grep in this chapter is scoped `--include='*.stx'`, so ~800 lines of imperative DOM are invisible to both. resources/assets/scripts/postline-analytics.ts alone is 403 lines with 35 vanilla hits (querySelector at 71/243/306/314, `.innerHTML =` at 95/105/115/126/134/144/165/184/275/292, addEventListener at 207/210/212/213/214). This is the only reason resources/views/analytics.stx scores clean on every grep.
  - **Fix:** Either move the module bodies into `<script client>` blocks on the pages that own them (analytics.stx for the chart, settings/accounts for the provider status fetches) so the validator and the greps can see them, or extend the chapter's CI task to cover `resources/assets/scripts/**/*.ts` as a fifth grep. The chart rendering in particular is `:text`/`@foreach` work, not innerHTML work.
- **resources/views/index.stx:152** — [8.5] *(silent)* 76 element-mutation sites in one file. Representative: `attachChip?.classList.add('hidden')` / `.remove('inline-flex')` at 152-153, reading UI state back out of the DOM via `button.classList.contains('bg-panel')` at 166, `count.textContent = ...` at 644 with `count.classList.toggle('text-red-700'|'text-amber-700'|'text-muted', ...)` at 651-653, and `.textContent` assignment at 306, 313, 322, 731.
  - **Fix:** `:class="{ hidden: !attachment(), 'inline-flex': !!attachment() }"` on the chip; `:text="countLabel()"` plus `:class="{ 'text-red-700': over().length > 0, 'text-amber-700': ..., 'text-muted': ... }"` on the counter. Line 166 in particular is the tell — deriving `activeMode` by asking the DOM which button has a background class means the class IS the state; make the signal the state and the class a binding.
- **resources/views/layouts/marketing.stx:1** — [2.2] *(silent)* The marketing layout is a full HTML document: `<!DOCTYPE html>` (1), `<html lang="en" class="text-ink bg-bg">` (2), `<head>` (3), `</head>` (91), `<body class="min-w-0 antialiased">` (92), `</body></html>` (96-97). It poisons `resources/views/welcome.stx`, the only page that extends it, in exactly the same way as the postline layout.
  - **Fix:** Same transform: strip the document wrapper, move lines 9, 18-20 (icon + font preconnects + Google Fonts stylesheet) and the `<style>` block at 22-90 into `@head … @endhead` with `@stack('styles')` appended, and delete the hand-written title/description/og/twitter tags at 7-8 and 12-15 in favour of `useSeoMeta` in welcome.stx.
- **resources/views/layouts/postline.stx:8** — [2.6] *(silent)* Page metadata is hand-written into the layout head as string slots — `<meta name="description" content="@yield('description', …)">` (line 7) and `<title>@yield('title', 'Postline')</title>` (line 8) — fed by `@section('title', …)` / `@section('description', …)` in each page (index.stx:3-4, queue.stx:3-4, timeline.stx:3-4, settings.stx:3-4, accounts.stx:3-4, analytics.stx:3-4). `grep -rn 'useSeoMeta\|useHead' resources/` returns zero and there is not a single `<script server>` block in resources/views. Consequence: the seven app pages ship NO og:*, NO twitter:*, NO canonical and NO robots — the layout head contains none of them. The chapter names this exact anti-pattern and says 'Delete the string-slot head; do not port it.'
  - **Fix:** Delete lines 7-8. In every page, replace `@section('title', …)` / `@section('description', …)` with a `<script server>` block calling `useSeoMeta({ title, description, canonical, ogType: 'website' })`. NOTE: this fix is inert until the layout stops being a document (2.2) — verified on 0.2.119, a `useSeoMeta({title})` under a doctype layout emits a SECOND `<title>` after the layout's, and the browser honours the first.
- **resources/views/layouts/postline.stx:1** — [1.4] *(silent)* The primary application layout lives inside pagesDir. Postline's own generated storage/framework/stx/route-types.d.ts:13 declares '/layouts/postline' as a typed route, and dist/layouts/postline.html is a 213,983-byte standalone HTML page containing the entire app shell, listed in dist/sitemap.xml as <loc>http://localhost/layouts/postline</loc>. Confirmed mechanism in 0.2.119: stx-router/dist/codegen.js generateRouteTypes has no exclusion filter, and ssg.js contains zero occurrences of excludeDirs (SSGConfig in ssg.d.ts:24-45 has no componentsDir/layoutsDir/partialsDir key at all).
  - **Fix:** git mv resources/views/layouts/postline.stx resources/layouts/postline.stx. @extends('layouts/postline') keeps resolving: utils.js resolveTemplatePathInner walks up from the page directory looking for a sibling `layouts/` dir and will find resources/layouts.
- **resources/views/marketing/README.md:1** — [1.4] *(silent)* An internal build-plan document ('Postline Landing Page — Build Plan', naming design skills and unshipped phases) sits inside pagesDir. serve.js discoverFiles globs '**/*.md' as well as '.stx' and '.html', and filters only ['layouts','components','partials'] plus the basenames of the configured dirs — 'marketing' is not excluded. I reproduced the exact filter from serve.js:9291-9309 against this repo and resources/views/marketing/README.md is in the discovered file list, i.e. it is served as a page by `buddy dev` and `buddy serve`. It is absent from route-types.d.ts only because stx-router's default extensions are ['.stx'].
  - **Fix:** Move it out of pagesDir — e.g. git mv resources/views/marketing/README.md docs/marketing-landing-plan.md — and rmdir resources/views/marketing.
- **resources/views/layouts/default.stx:1** — [1.4] *(silent)* Layout inside pagesDir. Typed as route '/layouts/default' at storage/framework/stx/route-types.d.ts:11, built to dist/layouts/default.html (32,151 bytes), and sitemapped as <loc>http://localhost/layouts/default</loc>. This file is also the one that should be satisfying rule 1.6c at resources/layouts/default.stx.
  - **Fix:** git mv resources/views/layouts/default.stx resources/layouts/default.stx — this single move satisfies both 1.4 and 1.6c.
- **resources/views/layouts/marketing.stx:1** — [1.4] *(silent)* Layout inside pagesDir. Typed as route '/layouts/marketing' at storage/framework/stx/route-types.d.ts:12, built to dist/layouts/marketing.html (32,664 bytes), and sitemapped as <loc>http://localhost/layouts/marketing</loc>.
  - **Fix:** git mv resources/views/layouts/marketing.stx resources/layouts/marketing.stx, then rmdir resources/views/layouts.
- **tsconfig.json:16** — [1.8] *(silent)* The `include` array covers only *.ts, *.d.ts, app/Actions, app/Models, app/Services and app/Support. Neither config/ nor resources/ nor the generated route types match anything. Verified: `bun x --bun tsc --listFilesOnly -p tsconfig.json` emits 1751 files with zero hits for config/ui.ts, zero for any postline/config/ path, and zero for any postline/resources/ path. Consequence: the `satisfies UiOptions` assertion at config/ui.ts:17 is never evaluated, so a misspelled key in config/ui.ts is a silent no-op; and storage/framework/stx/route-types.d.ts (which declares module "stx/routes" with the full RouteMap) is never loaded, so route patterns are untyped at every call site.
  - **Fix:**Add "config/**/*.ts", "resources/stores/**/*.ts" (and/or "resources/functions/**/*.ts" while that directory is the composables source) and "storage/framework/stx/route-types.d.ts" to tsconfig.json "include". Note the path is storage/framework/stx/, not .stx/ — Stacks overrides STX_DIR, and .gitignore:73 already lists storage/framework/stx/.
- **resources/views/index.stx:3** — [4.4] *(silent)* None of the six auth-walled app pages emits `<meta name="robots">`. Same defect at queue.stx:3, timeline.stx:3, settings.stx:3, accounts.stx:3 and analytics.stx:3 — every one of them is gated by the client auth guard in layouts/postline.stx:15-18 (`window.location.replace('/login')` when the auth-token cookie is missing) and every one renders with `robots: false`. Postline is advertising its entire authenticated surface as indexable, and the placeholder og tags from the finding above make those URLs look like generic shareable content.
  - **Fix:** Give each of the six views a `<script server>` block with `useSeoMeta({ title: 'Postline - Queue', description: '…', robots: 'noindex, nofollow' })`, replacing that page's `@section('title')`/`@section('description')` pair. `useSeoMeta({ robots })` is the only thing in the app that can emit the tag (head.js:41-42).
- **public/README.md:1** — [4.4] *(silent)* public/ ships no robots.txt (contents: audio, favicon.svg, images, README.md, svgs, videos), and no dist/robots.txt is produced either. The anchor here is public/README.md because the violation is a MISSING file. With no robots.txt and no per-page robots meta, nothing contradicts the sitemap, which actively lists /queue, /timeline, /settings, /accounts and /analytics.
  - **Fix:** Ship public/robots.txt disallowing the app surface — `User-agent: *` / `Disallow: /queue` / `Disallow: /timeline` / `Disallow: /settings` / `Disallow: /accounts` / `Disallow: /analytics` / `Disallow: /login` — plus a `Sitemap:` line pointing at the real origin once config/app.ts:15 carries a public URL rather than `postline.localhost`.
- **resources/views/layouts/postline.stx:1** — [4.1] *(silent)* The layout hand-writes the entire document shell: `<!DOCTYPE html>` (:1), `<html lang="en">` (:2), `<head>` (:3), `<meta name="description" content="@yield('description', …)">` (:7), `<title>@yield('title', 'Postline')</title>` (:8), closing `</head>` at :262. Six of the eight active views extend it. `hasDocumentShell` therefore returns true, `ensureDocumentShell` leaves the HTML untouched, and stx can only string-splice a runtime head in via `injectIntoHead` (process.js:679-701). I reproduced on the installed 0.2.119 using this exact layout: adding `useSeoMeta({ title: 'FROM-USEHEAD' })` to analytics.stx produces TWO title elements — `<title>Postline - Analytics</title> || <title>FROM-USEHEAD</title>` — and both the SPA fragment header (dev-server/serve-app.js:428) and the built client router (`doc.querySelector('title').textContent`) take the FIRST, i.e. the stale layout one. This is a trap armed for whoever migrates first, not a live symptom.
  - **Fix:** Delete lines 1-3, 7-8 and the closing `</head>`/`</body>`/`</html>` from the layout and move the shared tags into `config/ui.ts` `app.head.meta`/`app.head.link`; keep the layout's chrome (nav, sidebar, theme bootstrap scripts) as body content. Pages then own title/description through `useSeoMeta` in `<script server>`.
- **resources/views/layouts/marketing.stx:1** — [4.1] *(silent)* Second hand-written shell: `<!DOCTYPE html>` (:1), `<html>` (:2), `<head>` (:3), `<title>@yield('title', …)</title>` (:7), `<meta name="description">` (:8), og/twitter block (:12-15), closing `</head>` at :91. Note this is NOT dead code in Postline the way the chapter's marketing.stx was in bughq — `resources/views/welcome.stx:1` extends it, so it cannot simply be deleted. It is the only reason welcome.stx escapes the `stx Project` placeholder (its og:title at :13 triggers the injectSeoTags early return at seo.js:251), which means the placeholder bug is being masked by a second violation.
  - **Fix:** Strip the shell and the og/twitter block; move charset/viewport/favicon/Geist font links into `config/ui.ts` `app.head`, and let welcome.stx own title, description, ogImage and canonical through `useSeoMeta` + `useHead` in a `<script server>` block.
- **resources/views/login.stx:4** — [4.3] *(silent)* `@section('description', 'Sign in to your Postline workspace.')` is dead text. login.stx:1 extends `layouts/default`, and resources/views/layouts/default.stx is five lines — `@yield('content')`, `@yield('footer')`, `@stack('scripts')` — with no `<head>` and no `@yield('description')`. The section is never consumed. Rendered /login therefore has ZERO `<meta name="description">`: confirmed by processDirectives (`descs: []`) and by `grep '<meta name="description"' dist/login.html` returning nothing. The page still gets a correct `<title>Postline - Sign in</title>` because with no `<head>` in the output, autoShell's `generateDocumentShell` runs and picks up `context.__sections.title` (process.js:265) — so the title works and the description silently does not, which is exactly the kind of half-working state that hides for a year.
  - **Fix:** Replace login.stx:3-4 with a `<script server>` block calling `useSeoMeta({ title: 'Postline - Sign in', description: 'Sign in to your Postline workspace.', robots: 'noindex, nofollow' })`. That routes through `context.__stx_runtime_head`, which reaches generateDocumentShell regardless of what layouts/default.stx does or does not yield.
- **resources/views/index.stx:3** — [4.2] *(silent)* The title source is `@section('title', 'Postline - Composer')` — precedence tier 3 — resolved by `@yield` inside a hand-written layout head, rather than `useSeoMeta({ title })` in a `<script server>` block. Identical at queue.stx:3, timeline.stx:3, settings.stx:3, accounts.stx:3, analytics.stx:3, login.stx:3 and welcome.stx:3. The chapter's per-file 'more than one title source' loop passes cleanly (one source per page), so this is a mechanism violation, not a duplication one — but it is the root cause of the critical finding above: with no runtime-head title, `injectSeoTags` never hits its early return at seo.js:250 and the placeholder og/twitter block gets injected.
  - **Fix:** Per view, replace lines 3-4 with `<script server>` + `useSeoMeta({ title: 'Postline - Composer', description: 'Write, schedule, publish, and review social posts from a clean personal workspace.', ogImage: '…', robots: 'noindex, nofollow' })`. Keep `app.head.title: 'Postline'` in config/ui.ts as the bare last-resort word only.
- **config/ui.ts:16** — [3.12] *(silent)* partialsDir is 'resources/partials' → becomes 'resources/resources/partials'. Same double-prefix defect as :10 and :13.
  - **Fix:** Set `partialsDir: 'partials'`.
- **resources/views/components/FeatureCard.stx:1** — [3.3] *(silent)* A component inside pagesDir, with ZERO references anywhere in resources/, routes/ or app/ (verified by repo-wide grep excluding node_modules and dist). It is nonetheless built and published: dist/components/FeatureCard.html (35,065 B) with <title>stx App</title>, and <loc>http://localhost/components/FeatureCard</loc> in dist/sitemap.xml.
  - **Fix:** Delete the file. It is dead — the chapter reaches the same verdict for bughq's identically-named file.
- **resources/views/partials/theme.stx:1** — [3.3] *(silent)* A partial inside pagesDir, also with zero references (the theme bootstrap it duplicates was inlined into layouts/postline.stx:44-61 and login.stx:7-20). Built and published as dist/partials/theme.html (32,420 B, <title>stx App</title>) and listed as <loc>http://localhost/partials/theme</loc> in dist/sitemap.xml.
  - **Fix:** Delete the file, and delete the now-empty resources/views/partials/ directory.
- **resources/views/layouts/marketing.stx:1** — [3.3] *(silent)* Layout inside pagesDir; built to dist/layouts/marketing.html (38,053-byte-class artefact, rendered with an empty <title></title>) and listed as <loc>http://localhost/layouts/marketing</loc> in dist/sitemap.xml.
  - **Fix:** git mv resources/views/layouts/marketing.stx resources/layouts/marketing.stx.
- **resources/views/layouts/default.stx:1** — [3.3] *(silent)* Layout inside pagesDir; built to dist/layouts/default.html with <title>stx App</title> and listed as <loc>http://localhost/layouts/default</loc> in dist/sitemap.xml. In total dist/sitemap.xml carries 5 entries matching /(layouts|components|partials)/ where the chapter's CHECK requires 0.
  - **Fix:** git mv resources/views/layouts/default.stx resources/layouts/default.stx, then remove resources/views/layouts/ entirely.
- **resources/views/layouts/postline.stx:1** — [3.1] *(silent)* The app layout ships its own `<!DOCTYPE html>` (:1) and `<html lang="en" …>` (:2) plus a hand-rolled 259-line <head>. hasDocumentShell (dist/document-shell.js:64-66) therefore returns true for every page extending it, so dist/process.js:276 leaves the shell alone and :278 falls through to injectConfigHeadTags — which, verified in 0.2.119, is called with `baseHeadConfig` and NOT the merged `headConfig`. Consequence: app.head's title/lang/bodyClass/bodyAttrs are permanently unreachable, and any future `useHead({...})` in a `<script server>` on index/queue/timeline/settings/accounts/analytics is discarded with no error. (Postline currently makes zero useHead calls, so this is a live trap rather than an active regression; the layout-group metadata is unaffected here because @extends is explicit — see versionDrift.)
  - **Fix:** Delete lines 1-2 and 262 (`</head>`)/263 (`<body …>`)/510-511 (`</body></html>`); move charset/viewport/color-scheme/favicon and the two stylesheet <link>s into config/ui.ts `app: { head: { lang: 'en', link: [...], script: [...], bodyClass: '...' } }`; keep the theme/auth/router bootstrap scripts as `app.head.script` entries with `content`. The layout then starts directly at the shell <div> and the title flows from @section('title') via dist/process.js:265.
- **resources/views/layouts/marketing.stx:1** — [3.1] *(silent)* The marketing layout ships `<!DOCTYPE html>` (:1) and `<html lang="en" …>` (:2) with a 90-line hand-rolled <head>, including the Google Fonts preconnect/stylesheet trio at :18-20 that duplicates nothing today but cannot be shared with the app shell. Same mechanism as postline.stx: ensureDocumentShell is skipped and useHead output is dropped.
  - **Fix:** Delete lines 1-2 and 91-92/96-97; move the fonts <link>s, og:/twitter: metas and favicon into config/ui.ts app.head.link / app.head.meta (injectConfigHeadTags dedupes links by exact href and metas by name/property, so declaring them unconditionally is safe).
- **resources/views/layouts/marketing.stx:92** — [3.5] *(silent)* The marketing layout declares ZERO <main> elements — `<body>` at :92 goes straight to @yield('content') at :93 — and welcome.stx, its only consumer, declares none either. On /welcome, getContainer() (stx-router/dist/client.js:194-195) tries containerSel ('main', the default, since STX*ROUTER*OPTIONS is only set in postline.stx:62), then '[data-stx-content]', then 'main' again — all null. shouldIntercept then bails at client.js:844 (`if(!getContainer())return false;`), so SPA link interception is dead on the whole marketing surface and the `data-stx-link` attribute on welcome.stx:17 is inert.
  - **Fix:** Wrap the yield in the layout: `<main>@yield('content')</main>` in marketing.stx, exactly as the chapter's model marketing layout does. The chapter's CHECK requires exactly one <main> per layout file.
- **resources/views/login.stx:36** — [3.5] The login PAGE declares `<main class="grid place-items-center …" data-testid="login-page">` inside its @section('content'). Its layout (layouts/default.stx) declares none, so login is the only page supplying the router container for the 'default' group. It works today by accident; the moment a second page joins the group, or login gains a wrapper, the first-open/last-close extraction at bun-plugin-stx/dist/serve.js:10171-10172 spans the wrong region and the router injects chrome inside <main>.
  - **Fix:** Move the <main> into resources/layouts/default.stx wrapping @yield('content'), and change login.stx:36 to a `<div>` with the same classes (keep data-testid on the div).
- **resources/views/accounts.stx:116** — [6.1] *(silent)* Same as index.stx: a bare `<script>` with zero signals and zero directives across 562 lines. Six provider panels each maintain their connection state by writing textContent and toggling classes on `[data-*-channel-pill]` elements imperatively.
  - **Fix:** `<script client>` plus one `state()` per provider response and `derived()` for the pill label/state, bound via `:data-state="blueskyState()"` and `:text="blueskyPill()"` (the pattern settings.stx:39-41 already uses correctly).
- **resources/views/index.stx:700** — [6.6] Four raw fetch() calls (:700 link-preview, :857 queue/item, :903 queue/update, :961 queue create) with hand-rolled status/error handling written straight into DOM nodes. No caching, no cross-page sharing with queue.stx which fetches the same /api/postline/queue data.
  - **Fix:** `useQuery` for the link preview keyed on the URL (`useQuery(() => `/api/postline/bluesky/link-preview?url=${encodeURIComponent(url())}`)` refetches automatically when the dependency signal changes), and `useMutation('/api/postline/queue', { onSuccess: () => queue.invalidate() })` for create/update so queue.stx picks up the change without a manual refresh().
- **resources/views/index.stx:593** — [6.7] *(silent)* `draftSaveTimer = setTimeout(saveDraft, 100)` — a bare timer in a client script with no onDestroy. `config/ui.ts`-equivalent router config at layouts/postline.stx:62 swaps `[data-postline-workspace]` on navigation, so leaving / mid-keystroke leaves a timer that fires saveDraft against detached nodes.
  - **Fix:** `const draftSave = useDebounce(saveDraft, 100)` (auto-imported, verified present in 0.2.119 STX*AUTO*IMPORTS) or `useTimeout(saveDraft, 100)` which pairs its timer with onDestroy(stop) at dist/signals.js:3630.
- **resources/views/index.stx:719** — [6.7] *(silent)* `previewTimer = setTimeout(() => { void updateLinkPreview(post) }, 250)` — same pattern, and this one issues a network request. Navigating away within 250ms of typing a URL fires an authenticated /api/postline/bluesky/link-preview call whose result is written into a destroyed DOM tree.
  - **Fix:** Replace with `useDebouncedValue` on the URL signal feeding a `useQuery(() => …)` — useQuery fetches on onMount and its own cleanup is owned by the runtime, so nothing survives the swap.
- **resources/views/index.stx:1300** — [6.7] *(silent)* `window.addEventListener('beforeunload', saveDraft)` with no removal. The `root.dataset.composerReady` guard at :121-123 is set on the page fragment, which is a fresh element after every SPA swap, so the guard does not survive navigation — each return to / registers another beforeunload closure holding a reference to the previous composer's DOM.
  - **Fix:** `useEventListener('beforeunload', saveDraft)` — verified 0.2.119 dist/signals.js:4047-4054 registers onDestroy(removeEventListener) for you, which _handleStxLoad drains on the next stx:load.
- **resources/views/layouts/postline.stx:486** — [6.7] *(silent)* `window.stx?.useEventListener('keydown', …)` is called from a plain layout `<script>`, outside any component scope. Verified in 0.2.119: onDestroy with no currentLifecycleScope (dist/signals.js:721-728) pushes to the GLOBAL destroyCallbacks array, which _handleStxLoad drains and empties on the first stx:load (dist/signals.js:5657-5661). bindKeyboardShortcuts is guarded by `window.__postlineShortcutsBound` (:483-484) and is NOT re-registered on stx:load (unlike its four siblings at :502-505). Net effect: the 1-6 page shortcuts work on a cold load and stop working permanently after the first in-app navigation.
  - **Fix:** Either drop the `__postlineShortcutsBound` guard and add `window.addEventListener('stx:load', bindKeyboardShortcuts)` alongside the others, or move the binding into a `<script client>` on the layout so the onDestroy lands in the page scope and is re-registered per navigation.
- **resources/views/layouts/postline.stx:44** — [6.3] *(silent)* Copy 1 of 4 of the theme policy: reads localStorage 'theme', computes dark from matchMedia, writes html.dark + data-theme. Copy 2 at resources/views/login.stx:12-19 (identical, default 'system'). Copy 3 at resources/views/settings.stx:120-134 (applyTheme/setTheme plus the only persistence write). Copy 4 at resources/views/partials/theme.stx:2-6 — which defaults to 'light' instead of 'system' and ignores matchMedia entirely, a semantic divergence that only exists because the policy was copied.
  - **Fix:** `resources/stores/theme.ts` owning read/resolve/apply/persist, exposing `{ pref, isDark, set }`; each page does `const theme = useStore('theme')`. Keep only the pre-paint inline script (it must run before first paint) and have it call into nothing else. Delete partials/theme.stx — it is unreferenced (`@include` appears nowhere in resources) and its 'light' default contradicts the other three.
- **resources/views/index.stx:418** — [7.3] *(silent)* `variantSection.classList.toggle('hidden', !available)` — the per-network override panel's visibility is flipped by an imperative class write. The element is declared `class="hidden mb-3"` at index.stx:52, so the hidden sentinel and the layout utilities share one class list, exactly the fight rule 7.3 describes. This is one of 16 such sites in this file (152, 382, 418, 423, 458, 462, 664, 675, 733, 804, 830, 846, 969, 1185, 1212, 1317).
  - **Fix:** Declare `const variantsAvailable = state(false)` in a `<script client>` block and write `<div class="mb-3" style="display:none" x-show="variantsAvailable()">`. bindShow (signals.js:2287) reads the initial `display:none` as "starts hidden" and clears the inline style on reveal, so the `mb-3` utility is never contested. Delete the classList call.
- **resources/views/index.stx:124** — [7.0.5] `const root = document.querySelector('[data-testid="composer-page"]')` opens a 49-call chain of getElementById/querySelector element lookups in this one file (accounts.stx has 36, layouts/postline.stx 16 — 101 total). `ref=`/`@ref=` is used zero times in the repo.
  - **Fix:** Put `ref="composerRoot"` on the section and read it from scope; processRefAttributes (misc-directives.js:138) rewrites it to `data-stx-ref` and the runtime binds it at the signals-runtime ref site. For the elements whose only purpose is to receive text or a visibility flip, delete the lookup entirely and bind `:text` / `x-show` on the element instead.
- **resources/views/index.stx:664** — [7.0.7] Link-preview and post-row markup is assembled with `innerHTML`/`createElement`/`insertAdjacentHTML` (15 sites in this file) rather than an `x-for` over a signal array, so the list is invisible to the reactive graph and is rebuilt wholesale on every change.
  - **Fix:** Model the rows as `const posts = state([])` and render `<article class="…" x-for="post in posts()">…</article>` with `:text` / `:href` / `:data-*` on the children — the shape queue.stx:39-56 and timeline.stx:51-90 already use successfully in this same repo. bindFor (signals.js:2602) clones and rebinds per item.
- **resources/views/accounts.stx:141** — [7.0.4] Channel handles, status pills and detail lines are refreshed with 25 imperative `textContent` writes (index.stx adds 19 more). None of the six status pills declared at accounts.stx:44,55,67,79,90,105 carry a `:text` binding, so their content is entirely outside the reactive graph.
  - **Fix:** `const blueskyHandle = state('Not connected')` in `<script client>`, then `<p class="my-1.5 text-[13px] text-body" :text="blueskyHandle()">Not connected</p>` — static text is the SSR fallback, `:text` overwrites on hydration, no cloak. settings.stx:39,41,54 is the working example in this repo.
- **resources/views/accounts.stx:238** — [7.3] *(silent)* `linkedinConnectEnv.classList.toggle('hidden', !data?.configuredFromEnv)` — the "Use token from .env" button's visibility is a class flip. Declared `class="hidden text-muted text-xs underline …"` at accounts.stx:57. Repeated identically at 336 (twitter, markup line 109), 435 (instagram, markup line 69) and 531 (threads, markup line 81).
  - **Fix:** `const linkedinFromEnv = state(false)` plus `<button class="text-muted text-xs underline hover:text-zinc-700 transition-colors" style="display:none" x-show="linkedinFromEnv()" @click="useLinkedinEnvToken" type="button">Use token from .env</button>`. Drop the classList call and the querySelector that feeds it.
- **resources/views/layouts/postline.stx:319** — [7.3] *(silent)* Sidebar collapse toggles `hidden` on nav elements (319, 322, 390, 409) and additionally writes `row.style.display` directly (384, 391, 403, 410, 412) — two competing hand-rolled visibility mechanisms in the shell layout that every active page extends.
  - **Fix:** Hoist the collapse boolean into a signal (`const sidebarCollapsed = state(…)`) in a `<script client>` block and drive the nav rows with `x-show="!sidebarCollapsed()"`; bindShow owns `style.display` and restores the original value, so nothing needs to remember what `display` was.
- **config/ui.ts:8** — [9.13] *(silent)* config/ui.ts exports only `componentsDir`/`layoutsDir`/`partialsDir`. There is no `strict` key, so `validateClientScript` runs with `strictConfig = { enabled: false }` (script-validation.js:129) and every one of the 12 navigation violations below passes silently at render time. The rules that would catch them already exist verbatim in the framework: "window.location is prohibited → Use navigate() or useRoute()" (script-validation.js:47-51), "window.history is prohibited → Use navigate(), goBack(), goForward()" (:52-56), and location.href/assign/replace (:113-125).
  - **Fix:** Add to the default export in config/ui.ts: `strict: { enabled: true, failOnViolation: true, allowPatterns: ['location.replace() is prohibited'] }`. The allowPattern covers the one legitimate pre-paint auth bounce at resources/views/layouts/postline.stx:17; land `enabled: true` first (warns), fix the sites below, then add `failOnViolation: true`. Type is `strict?: boolean | StrictModeConfig` — verified at node_modules/@stacksjs/stx/dist/types/config-types.d.ts:311, shape at :214-218.
- **resources/views/layouts/marketing.stx:92** — [9.9] *(silent)* The marketing layout is a full `<!DOCTYPE>` document whose `<body>` is `@yield('content')` + `@yield('footer')` + `@stack('scripts')` — it contains ZERO `<main>` elements, and `resources/views/welcome.stx` actively extends it (welcome.stx:1). welcome.stx also has no `<main>`. So the rendered `/welcome` document has no router container at all: `containerSel` is `'main'` there (marketing.stx never sets `window.STX_ROUTER_OPTIONS`, and config/ui.ts has no router block, so the serve default `container:'main'` applies), and `getContainer()` (client.js:194) returns null. Consequence verified in 0.2.119: `shouldIntercept()` bails at client.js:844 (`if(!getContainer())return false`), and for the four anchors that DO carry `data-stx-link` (welcome.stx:17, :37, :196, :215 → /login) the handler skips `shouldIntercept` entirely (client.js:856), calls `preventDefault()` (:866), runs `navigate()` → fetch → `swap()` → `if(!currentContent){location.href=url;return}` at client.js:394-395. Every "SPA" CTA on the landing page therefore costs a full round-trip fetch that is thrown away, plus a hover-prefetch of the same document, and then full-navigates anyway.
  - **Fix:** Wrap `@yield('content')` in the marketing layout's `<body>` in a single `<main>`: `<body class="min-w-0 antialiased"><main>@yield('content')</main>@yield('footer')@stack('scripts')</body>`. The layout owns the container; welcome.stx keeps zero `<main>`s.
- **components/PostlineSidebar.stx:103** — [9.5b] *(silent)* `aria-current="{{ link.active ? 'page' : 'false' }}"` is server-rendered on all four nav-link sites (:41, :60, :103, :127), and `link.active` is computed server-side from the request pathname. But the sidebar sits OUTSIDE the router container: layouts/postline.stx:62 sets `container: '[data-postline-workspace]'`, which is the `<section>` at postline.stx:278, while `<PostlineSidebar />` is rendered at postline.stx:265, a sibling. All app pages share `layouts/postline`, so `isLayoutChange` is false (client.js:620) and only the container's `innerHTML` is swapped (client.js:668-673) — the sidebar DOM is never re-rendered. Neither `updateNav()` (client.js:901-911) nor `updateActiveLinks()` (client.js:913-928) touches `aria-current`; they only add/remove classes. Result: after any in-app SPA navigation, `aria-current="page"` stays on the previously-visited link and is never applied to the new one, so a screen reader reports the wrong current page for the entire session until a hard reload.
  - **Fix:** Drive `aria-current` from the same reactive source the router uses instead of the server snapshot: in the layout's client script, on `stx:navigate`/`stx:load`, set `aria-current` on `[data-stx-nav] a[data-stx-link]` by comparing `useRoute().path` to each `href` (exact for `/`, prefix otherwise) — mirroring the `isExact`/`isActive` logic at client.js:921-922. Keep the server-rendered value as the first-paint state.
- **resources/views/accounts.stx:254** — [9.7] *(silent)* `window.location.href = '/api/postline/linkedin/auth'` inside a click handler. Identical sites at accounts.stx:354 (twitter), :451 (instagram), :547 (threads). These are the OAuth kick-offs — a real document navigation IS required here (a fragment fetch would swallow the provider 302, exactly as §9.2 case 4 describes) — but the chapter's exemption table does not cover them and the raw form is what `script-validation.js:113-115` is written to reject.
  - **Fix:** `navigate('/api/postline/linkedin/auth', true)` — `navigate(url, forceReload)` with `forceReload` truthy is literally `window.location.href = url` (verified signals.js:966-970), so behaviour is byte-identical while going through the sanctioned, auto-imported, lintable API. Better still per §9.2, render these as markup: `<a href="/api/postline/linkedin/auth" data-no-router class="…">Connect LinkedIn</a>` with the reason stated inline, which also removes the `addEventListener` binding.
- **resources/views/accounts.stx:288** — [9.7] *(silent)* `window.history.replaceState({}, '', `${window.location.pathname}${query ? `?${query}` : ''}`)` used to strip OAuth callback params from the URL. Identical sites at accounts.stx:387, :483, :579. §9.7 states `history.replaceState()` is never permitted; script-validation.js:52-56 carries the matching rule ("window.history is prohibited → Use navigate(), goBack(), goForward()").
  - **Fix:** There is NO exact stx-native replacement in 0.2.119 — see versionDrift. `useSearchParams()` (signals.js:1024-1046) exposes only `data`/`get`/`set`/`setAll`, has no `delete`, and its `set` uses `history.pushState`, which adds the history entry this code deliberately avoids. The honest options are (a) have the OAuth callback action 302 back to a clean `/accounts` so the params never reach the browser — the correct fix, and it also satisfies §9.8's principle; or (b) keep `replaceState` and add `'window.history is prohibited'` to `strict.allowPatterns` with the reason stated inline, so the exemption is declared rather than silent.
- **config/ui.ts:8** — [10.1] *(silent)* The default export declares componentsDir/layoutsDir/partialsDir and no `plugins` key, so `loaded._pluginComponentDirs` is never populated (config.js:420 gate) and none of the 86 components in 44+ families under node_modules/@stacksjs/components/src/ui resolve by tag name. `grep -n "plugins" config/ui.ts` returns zero hits. This is why every UI primitive in Postline is hand-rolled: 27 distinct button class strings, 5 distinct pill strings, a hand-rolled avatar, a hand-rolled auth form, and 6 "Loading…" placeholders where `<Skeleton>`/`<Spinner>` exist.
  - **Fix:** Add `plugins: ['@stacksjs/components/stx-plugin'],` as the first key of the default export. The `./stx-plugin` subpath export exists in @stacksjs/components@0.2.119 and the shim resolves `./src/ui`. At boot, config.js:463 will print `[stx] Plugin loaded: @stacksjs/components`. Note that the 32 tags quarantined by §10.3 are still broken in 0.2.119 — verified — so adopt only Button, Badge, Avatar, Switch, Spinner, Skeleton, Progress, Card, Tooltip, Dialog (without DialogPanel), Table*, Sidebar*, Notification, TextInput/PasswordInput/Checkbox/Radio/Textarea.
- **config/ui.ts:10** — [10.1] *(silent)* `componentsDir: 'resources/components'` (and `layoutsDir: 'resources/layouts'` at line 13, `partialsDir: 'resources/partials'` at line 16) are already root-relative, but config.js:411-418 prefixes each with the resolved `root` ("resources") because `root !== '.'`. The effective values become `resources/resources/components`, `resources/resources/layouts` and `resources/resources/partials` — all three directories are absent from the repo. This is visible verbatim in the build error's searched-paths list. Local component resolution therefore works only by accident, via the hardcoded fallback dirs. The chapter's own §10.1 RIGHT block reproduces this bug.
  - **Fix:** Set these to paths relative to the stx root, i.e. `componentsDir: 'components'`, `layoutsDir: 'layouts'`, `partialsDir: 'partials'` (which then resolve to resources/components, resources/layouts, resources/partials) — or pin `root: '.'` explicitly and keep the current values. Verify by adding `debug: true` and checking that no `unresolved component tag(s)` warning is printed.
- **resources/views/layouts/postline.stx:16** — [12.9-step4] *(silent)* 34 prohibited DOM calls in the shell every app page inherits, so the violation multiplies across all seven layout-extending views. :16 document.cookie; :17 window.location.replace; :28 document.cookie; :32/:491 window.location; :50/:55/:332/:338/:380 window.localStorage; :296-300 document.querySelector ×5; :318/:321/:341/:350/:358/:363/:389/:408/:422 document.querySelectorAll ×9; :361/:366 window.history; :502-506 window.addEventListener ×5.
  - **Fix:** useCookie() for :16/:28; navigate() for :17/:32/:491 (or keep :17 and list 'location.replace()' in strict.allowPatterns, since a pre-paint auth guard must beat first paint); useLocalStorage() for the five localStorage reads/writes; useRef("name") for all 14 querySelector/querySelectorAll calls; goBack()/goForward() for :361/:366; useEventListener() for the five window.addEventListener calls.
- **resources/views/accounts.stx:118** — [12.9-step4] *(silent)* 25 prohibited DOM calls in one of the six active app pages. :118 document.querySelector; :254/:354/:451/:547 `window.location.href = …` (both the window.location and location.href-assignment rules fire on each); :276/:375 window.location.search; :288/:387/:483/:579 window.history.replaceState (plus window.location on the same lines); :293/:392/:488/:584/:658/:670 document.addEventListener('DOMContentLoaded', …) ×6.
  - **Fix:** useRef("name") for :118; navigate('/api/postline/<provider>/auth') for the four href assignments; useSearchParams() for the two window.location.search reads and the four history.replaceState query-cleanups; replace the six DOMContentLoaded listeners with onMount() inside the `<script client>` setup, which stx already runs after hydration.
- **resources/views/index.stx:120** — [12.9-step4] *(silent)* 21 prohibited DOM calls in the composer, the app's primary page. :120 document.querySelector; :235/:297/:301/:304/:308/:319/:330/:337/:345/:530 document.createElement ×10 — an entire DOM-building layer hand-rolled in vanilla JS; :178/:908/:1313 window.location; :592/:718 clearTimeout and :593/:719 setTimeout; :908 location.assign; :1300 window.addEventListener('beforeunload'); :1328 document.addEventListener('DOMContentLoaded').
  - **Fix:** The ten createElement sites are the real work: replace the imperative row/post builders with x-for over a signal array so stx renders them from the template. useRef("name") for :120; useSearchParams() for :178/:1313; navigate('/queue') for :908; useDebounce() for the draft-save pair at :592-593 and the preview pair at :718-719; useEventListener() for :1300; onMount() for :1328.
- **resources/views/index.stx:8** — [12.5] *(silent)* An 8-line `<!-- -->` comment documenting the Crosswind safelist, containing four backticks (`display:none`, `grid`, `min-h-20`, `p-3`). It trips stx's own `stx/no-backticks-in-html-comments` at index.stx:12:9 and the chapter's narrow banned-token gate. I rendered index.stx through processDirectives on 0.2.119 and confirmed the merged `__stx_setup_` function is intact and the `data-stx` body binding survives — so this is a latent risk, not a live break today — but the comment ships verbatim to every visitor and is one `<html>` or `<script` away from suppressing the document shell.
  - **Fix:** Change the delimiters to `{{-- … --}}`. The comment body needs no other edit — `{{-- --}}` is compiled away before render and safely contains `<main>`, `<head>`, `<script>` and backticks.
- **resources/views/login.stx:45** — [12.5] *(silent)* A 4-line `<!-- -->` comment explaining the pre-hydration `display:none` state, containing backticks around `grid` and `hidden`. Fires stx/no-backticks-in-html-comments at login.stx:47:38 and the narrow gate. Ships to the browser on the sign-in page.
  - **Fix:** Convert to `{{-- … --}}`.
- **resources/views/timeline.stx:74** — [12.5] *(silent)* A 3-line `<!-- -->` comment explaining the x-html choice, containing a backtick around `linkifyBody`. Fires stx/no-backticks-in-html-comments at timeline.stx:75:41 and the narrow gate.
  - **Fix:** Convert to `{{-- … --}}`.
- **package.json:21** — [12.3a] *(silent)* No script, CI step, or committed file anywhere in Postline invokes `lintStxStrict`, `PROHIBITED_DOM_PATTERNS`, or `scanScriptTags`. stx's footgun linter has never been run against this codebase. Running it now: 14 diagnostics raw (7 no-bare-function-ref-in-event, 4 no-view-level-script-client, 3 no-backticks-in-html-comments); 3 survive re-triage against 0.2.119.
  - **Fix:** Add a committed `scripts/lint-stx.ts` and a `"lint:stx"` package script that globs resources/**/*.stx through lintStxStrict, and wire it into .github/workflows/ci.yml alongside the lint job. Pin the toggles with the citation inline, exactly as 12.3 requires: `'stx/no-bare-function-ref-in-event': false` (bare refs handled at dist/signals.js:1398-1404 inside parseEventShorthand, defined :1325, called :1464/:2153 — re-verified against 0.2.119) and `'stx/no-view-level-script-client': false` (view-level `<script client>` compiles to a normal __stx*setup* function — re-verified by rendering login/timeline/queue/settings through processDirectives on 0.2.119). Do NOT act on those 14 raw diagnostics.
- **.github/workflows/ci.yml:47** — [12.9] *(silent)* The only lint enforcement is `bun run lint`, which resolves to `./buddy lint` and passes no `--max-warnings`; pickier 0.1.44's default is -1, i.e. no gate. There is no stx-strict step, no DOM-guard step, no landmine step, no grep-gate step, and no doctor step anywhere in CI. The 12.9 gate script does not exist in the repo (scripts/ contains only seed-user.ts), so nothing distinguishes a regression from inherited debt.
  - **Fix:** Commit the 12.9 gate script (e.g. scripts/stx-gate.sh) verbatim, adapted to Postline's paths, and add a `stx-gate` job to ci.yml that runs it. Change the lint step to `bunx --bun pickier . --max-warnings 0`. Record today's measured baseline in the script's header comment so the next agent can tell new violations from old ones.
- **resources/views/layouts/marketing.stx:29** — [11.2] *(silent)* A SECOND, divergent copy of the same token set (lines 29-41). --bg is #f4f4f2 here but #f3f3f1 in postline.stx:77; --panel-2 is #ececeb vs #fafafa; --panel-3 is #e6e6e4 vs #f4f4f5; --accent is #10b981 (emerald) vs #0a0a0a (near-black). So `bg-accent` renders green on /welcome and near-black on /, and this copy has no dark variant at all. Both files must be edited in lockstep forever.
  - **Fix:** Delete this :root block. Express the marketing palette as a single scoped preflight in config/crosswind.ts (e.g. `[data-postline-marketing]{--accent:#10b981;...}`) and put `data-postline-marketing` on the marketing <body>, so there remains exactly one file that declares a token value.
- **resources/views/layouts/postline.stx:330** — [11.4] *(silent)* `collapseIcon?.classList.toggle('rotate-180', collapsed)`. The literal `rotate-180` appears nowhere in a class= attribute in the entire repo (verified: grep -rn 'rotate-180' returns only this line), and config/crosswind.ts has no `safelist`, so Crosswind never emits a `.rotate-180` rule. The element it targets is components/PostlineSidebar.stx:72, a <span class="... i-hugeicons-sidebar-left"> — not an SVGElement, so the `instanceof SVGElement` branch at line 327 is never taken and this classList path always runs. The collapse icon never flips.
  - **Fix:** Add `safelist: ['rotate-180']` to config/crosswind.ts, or put `rotate-180` behind a static attribute selector already covered by the layout's preflight, or drop the SVGElement branch and always set style.transform.
- **config/crosswind.ts:14** — [11.4] *(silent)* No `safelist` key exists, yet 102 sites across resources/ and components/ compute class strings in the browser via classList.add/remove/toggle or `el.className = '...'` — the exact case the chapter says MUST be safelisted. Postline has zero `:class`/`x-class` attributes, so the chapter's own CHECK reports clean; the exposure moved to plain classList/className calls, which the extractor cannot see at all.
  - **Fix:** Add a `safelist: [...]` to config/crosswind.ts enumerating every utility applied only from script, and delete the hand-rolled safelist <div> at resources/views/index.stx:16. The generated sheet force-generates each safelist entry at dev-server/crosswind.js:314-315.
- **resources/views/layouts/marketing.stx:22** — [11.1] 69 lines of CSS (22-90) in the marketing layout: token block, base html/body rules, a hand-written `.font-mono` at line 52 that shadows a Crosswind-generated utility of the same name (whichever wins depends on sheet order), ::selection and :focus-visible rules, and two named @keyframes blocks (67-88).
  - **Fix:** Tokens → preflights. `.font-mono` → `theme.extend.fontFamily.mono` in config/crosswind.ts so the utility is generated rather than overridden. ::selection, :focus-visible and the reveal-up/marquee @keyframes are legitimate hand-written CSS per the chapter's table — move them into `preflights`, not a <style> tag.
- **resources/views/layouts/postline.stx:260** — [11.7] Two render-blocking external stylesheets linked into every app page: /assets/styles/postline-toast.css (292 lines, 19 bespoke classes) and /assets/styles/postline-timeline.css (286 lines, 32 bespoke classes). 578 lines / 51 classes of utility-composition CSS living outside Crosswind. `ls public/*.css` prints nothing so the chapter's literal check passes, but the violation is the same — it just moved to resources/assets/styles/.
  - **Fix:** Convert the 51 classes to `shortcuts` in config/crosswind.ts (they are utility compositions: .postline-toast is grid/gap/p/rounded/border/shadow). The genuinely un-expressible parts — the enter/leave transform transitions on .is-visible/.is-leaving and any ::-webkit-* selectors — go to `preflights`. Then delete both <link> tags and both files.

### medium (86)

- **resources/views/accounts.stx:254** — [8.7] *(silent)* `window.location.href = '/api/postline/linkedin/auth'` inside a click handler, repeated at 254, 354 (twitter), 451 (instagram) and 547 (threads). By rule 8.11's mechanical test these run inside a handler, so the router is live and they are violations, not exceptions.
  - **Fix:** `navigate('/api/postline/linkedin/auth', true)` — the declared full document load. navigate with forceReload sets location.href itself (signals.js:967-969), so behaviour is byte-identical, but it is one declared API and one fewer strict violation.
- **resources/views/index.stx:908** — [8.7] *(silent)* `window.location.assign('/queue')` in the `.then`-equivalent continuation of an async queue-update handler, discarding the SPA transition on an in-app navigation that has no reason to be a document load.
  - **Fix:** `navigate('/queue')` — no second argument. This is an in-shell navigation to a route the router already owns; it should keep the fragment fetch and the transition.
- **resources/views/index.stx:540** — [8.9] *(silent)* Raw localStorage for the composer draft: `JSON.parse(localStorage.getItem(draftKey) || 'null')` at 540 and `localStorage.setItem(draftKey, JSON.stringify({...}))` at 578, wrapped in hand-written try/catch at 539/565 and 576/588. Both are invisible to strict mode — the validator's pattern is `/window\.localStorage/` (script-validation.js:63), so a bare `localStorage.` never matches.
  - **Fix:** `const draft = useLocalStorage('postline:composer-draft:v1', null)` — it JSON.parses on read and JSON.stringifies on write (signals.js:3878+), which is exactly the shape already stored here, so unlike the token case in the chapter this one migrates cleanly with no legacy-value hazard. The two try/catch blocks and the manual serialisation both delete.
- **resources/views/layouts/postline.stx:16** — [8.9] *(silent)* `document.cookie` read directly in the pre-hydration auth guard at 16 and again in the fetch bearer-bridge at 28 (`document.cookie.match(/(?:^|;\s*)auth-token=([^;]+)/)`). The validator flags `document.cookie` with no read/write distinction, so these two lines are what will fire first when strict is enabled.
  - **Fix:** Line 16 is a genuine pre-paint guard and belongs in the 8.11 declared set — keep the regex test but note it in a comment as the one sanctioned site. Line 28 is not pre-paint in the same sense: it runs for the lifetime of the page, so replace `readToken` with `const authToken = useCookie('auth-token')` and read `authToken()`. That also removes the manual `decodeURIComponent` at 36, which is useCookie's default decoder.
- **resources/views/settings.stx:123** — [8.5] *(silent)* applyTheme mutates the root element by hand: `document.documentElement.classList.toggle('dark', dark)` at 123 and `.setAttribute('data-theme', ...)` at 124, plus raw `localStorage.getItem('theme')` at 93 and `setItem` at 130. The file carries a 14-line comment (106-119) documenting exactly why useColorMode cannot express this, which is the right way to declare an exception — but the raw localStorage calls at 93/130 are not covered by that reasoning and could go through useLocalStorage regardless.
  - **Fix:** Keep the classList/setAttribute pair with its existing justification comment (the useColorMode `applyDOM` limitation is real and verified), but replace lines 93 and 130 with `const themePref = useLocalStorage('theme', 'system')` — that removes the two try/catch blocks and makes the signal and the persisted value the same object. Then `setTheme` is `themePref.set(pref); applyTheme(pref)`.
- **resources/views/login.stx:107** — [8.7] *(silent)* `location.assign('/')` inside `storeTokenAndEnter`, called from the async submit continuation at 135 and 143. The comment at 97-103 argues correctly that a fresh document is wanted here, but 8.7's ruling is that a deliberate full load is spelled `navigate(url, true)`, not a raw call.
  - **Fix:** `navigate('/', true)`. Identical behaviour (signals.js:967-969 sets location.href when forceReload is truthy), one declared API, and it stops being a strict violation so the report stays signal.
- **resources/views/settings.stx:147** — [8.7] *(silent)* `location.assign('/login')` inside the `signOut` handler. Same pattern as login.stx:107 — runs inside a handler with the router live, so 8.11's test classes it as a violation rather than a declared exception.
  - **Fix:** `navigate('/login', true)`. The comment at 140-144 already argues for a hard navigation; forceReload is the declared way to say that.
- **package.json:1** — [8.10] *(silent)* No task wires the validator (grep A) plus the element-mutation grep (B) and the bare-globals grep (C) into one command, and no CI workflow references them. The four blind-spot categories therefore have no enforcement at all, and grep B currently returns 197 hits with nothing watching it.
  - **Fix:** Add a `"lint:stx"` script running the chapter's burn-down bun -e snippet plus greps B and C (extended to cover resources/assets/scripts/**/*.ts, per the fifth blind spot), exiting non-zero on any hit, and call it from .github/workflows/ci.yml alongside the existing lint step.
- **resources/views/queue.stx:117** — [2.7] *(silent)* A bare 29-line `<style>` block (117-146) sits inside `@section('content')` with no `@push`. There is no `@push`/`@prepend` and no `@stack('styles')` anywhere in the repository — `grep -rn '@push\|@prepend'` over all .stx files returns nothing, and the only stacks that exist are `@stack('scripts')` (postline.stx:509, marketing.stx:95, default.stx:5) and `@stack('head')` (Desktop.stx:30), all of them unfilled and therefore rendering as ''. The CSS lands inside `<main>` in the body instead of `<head>`, which is a FOUC on cold load for every status-pill and view-toggle rule.
  - **Fix:** Wrap as `@push('styles') <style>…</style> @endpush` and add `@stack('styles')` inside the layout's new `@head … @endhead` block (postline.stx). Shared status-pill/view-toggle rules are duplicated in spirit across queue/settings and belong in a real stylesheet next to /assets/styles/postline-*.css.
- **resources/views/settings.stx:68** — [2.7] *(silent)* A bare `<style>` block (68-88) inside `@section('content')`, no `@push`. Same mechanism and same consequence as queue.stx.
  - **Fix:** `@push('styles') … @endpush`, with `@stack('styles')` added inside postline.stx's `@head` block.
- **resources/views/login.stx:21** — [2.7] *(silent)* A bare `<style>` block (21-35) carrying the entire dark-theme override layer for the login page, inside `@section('content')`, no `@push`. login.stx extends `layouts/default` (a correct bare fragment), so this page DOES get a synthesised shell — which makes the omission more costly, not less: the overrides are appended to the body after the shell's stylesheet, so a dark-mode user gets a light flash before these rules apply.
  - **Fix:** `@push('styles') <style>…</style> @endpush` and add `@head @stack('styles') @endhead` to resources/views/layouts/default.stx (which currently has only @yield/@yield/@stack('scripts')).
- **config/ui.ts:13** — [2.12] *(silent)* `layoutsDir: 'resources/layouts'` but the real layouts (postline.stx, marketing.stx, default.stx) live in `resources/views/layouts/`. `resources/layouts/` contains exactly one file — the unused desktop-demo `Desktop.stx` — and no `default.stx`, so the `<layoutsDir>/<defaultLayout>.stx` fallback resolves a path that does not exist. Verified this is masked the same way the chapter describes: node_modules/@stacksjs/actions/dist/dev/views.js:60-62 resolves `layoutsDir` via `firstExistingPath(['resources/views/layouts', 'resources/layouts'])` and hands it to serve() at views.js:72, which takes precedence over the config value. config/ui.ts:10 `componentsDir: 'resources/components'` is likewise overridden (views.js:71 hardcodes the framework default).
  - **Fix:** Pick one direction, not both: either set `layoutsDir: 'resources/views/layouts'` in config/ui.ts:13, or move postline/marketing/default.stx into `resources/layouts/` alongside (or instead of) Desktop.stx. Moving is preferable — it puts layoutsDir/partialsDir/componentsDir on the same level and drops the dependence on the route-exclusion list at bun-plugin-stx/dist/serve.js:9298 that currently keeps resources/views/layouts/*.stx from being served as pages.
- **resources/views/partials/theme.stx:1** — [1.4] *(silent)* A partial inside pagesDir. Typed as route '/partials/theme' at storage/framework/stx/route-types.d.ts:14, built to dist/partials/theme.html and sitemapped as <loc>http://localhost/partials/theme</loc>. It also has zero references — grep for '@include', '@partial' and '@component' across resources/ returns nothing, and the theme bootstrap it contains is hand-reimplemented inline at resources/views/layouts/postline.stx:50-53. Additionally it is in the wrong place for the resolved partialsDir: dev resolves partialsDir to 'resources/partials' (firstExistingPath in @stacksjs/actions/dist/dev/views.js:62-66 prefers the first candidate that contains templates, and resources/partials has two .stx files), so an @include('theme') would never find it.
  - **Fix:** Delete it (zero references, duplicated inline), or git mv resources/views/partials/theme.stx resources/partials/theme.stx and @include it from resources/views/layouts/postline.stx in place of the inline script. Then rmdir resources/views/partials.
- **resources/views/components/FeatureCard.stx:1** — [1.4] *(silent)* A component inside pagesDir. Typed as route '/components/FeatureCard' at storage/framework/stx/route-types.d.ts:7, built to dist/components/FeatureCard.html (35,065 bytes), and sitemapped as <loc>http://localhost/components/FeatureCard</loc>. It has zero references anywhere in resources/, routes/ or app/.
  - **Fix:** git rm resources/views/components/FeatureCard.stx (zero references), then rmdir resources/views/components. If it is wanted, git mv it to resources/components/ instead — the serve path resolves user components via the convention fallback in @stacksjs/stx/dist/utils.js (userComponentFileExists pushes resources/views/components then resources/components off options.root, which serve.js sets to process.cwd()).
- **config/ui.ts:17** — [1.10] *(silent)* The exported object stops at three keys. Missing and therefore left to whatever each loader defaults: `ssr` (Loader A resolves it to false, Loader B to true — verified; the false side is what makes `stx build` pick SSG over SSR, which is consistent with dist/ containing SSG output including the non-page pages above), `plugins` (unset, so _pluginComponentDirs is never populated — the only code path that registers the shipped component library, config.js:420-468), `strict` (unset, so stx's prohibited-DOM-API validator for client <script> blocks never runs — directly relevant given resources/views/layouts/postline.stx:50-53 contains raw window/document/localStorage), `publicDir`, `router`, and `app` (unset, so every built page carries stx's placeholder SEO — dist/layouts/postline.html line 1 literally reads `<meta name="title" content="stx Project">` and `content="A website built with stx templating engine"`).
  - **Fix:** Adopt the 1.10 template: add root, pagesDir, storesDir (or composablesDir), publicDir: 'public', ssr: true, plugins: ['@stacksjs/components/stx-plugin'], strict: true, router: { container: 'main', interceptAllLinks: true }, and app: { head: { title: 'Postline' } }, each with the explanatory comment block. `container: 'main'` matches the existing <main> at resources/views/layouts/postline.stx:279.
- **config/ui.ts:17** — [1.7a] *(silent)* `storesDir` is undeclared and no resources/stores directory exists. store-loader.js:9-11 therefore resolves path.resolve(config.root, 'stores') = resources/stores, the glob throws, and getStoreScript returns null — no <script data-stx-stores> is ever emitted. Confirmed: zero files in dist/ contain 'data-stx-stores'.
  - **Fix:** Either declare `storesDir: 'resources/stores'` and move the shared reactive state there, or — given 0.2.119 (see versionDrift) — keep resources/functions and declare `composablesDir: 'resources/functions'` so the directory stops depending on the inferred root. Pick one; do not leave both undeclared.
- **package.json:56** — [1.2-CI] *(silent)* No scripts/check-stx-topology.ts exists (scripts/ contains only seed-user.ts) and nothing in .github/workflows/ or package.json references a topology check. The two-loader disagreement is invisible to every existing gate: pickier does not read it, `bun run typecheck` does not visit config/ui.ts at all (see 1.8), and the app boots because both servers pass directory overrides on top.
  - **Fix:** Add scripts/check-stx-topology.ts from chapter 1.2 (copy the `served` values before calling loadStxConfig — bunfig caches and mutates the same object; I hit this in reverse while auditing and got a false 'defaultLayout: undefined'), point its pagesDir sub-check at storage/framework/stx rather than .stx, and wire it into .github/workflows/ci.yml.
- **resources/views/layouts/marketing.stx:13** — [4.11] *(silent)* og:title (:13) and og:description (:14) are hand-typed from the same `@yield` that already fills `<title>` (:7) and `<meta name="description">` (:8) — the exact 're-typed instead of derived' pattern the rule bans, and the reason the two pairs can silently drift the moment someone edits one `@yield` default and not the other (they have ALREADY drifted: :8's default sentence ends 'Self-hosted, open, built on Stacks.' while :14's does not). Line :15 then declares `twitter:card: summary_large_image` with no `og:image` and no `twitter:image` anywhere in the repo, so the large-image card renders blank. `public/images/og-image.png` exists and is referenced by nothing.
  - **Fix:** Delete lines 12-15 from the layout. In welcome.stx's `<script server>`, call `useSeoMeta({ title, description, ogImage: 'https://<origin>/images/og-image.png', twitterCard: 'summary_large_image' })` — head.js:43/:58 derive og:title, og:description, twitter:title, twitter:description and `twitterImage ?? ogImage` from those two fields, so the four tags cannot drift.
- **resources/views/welcome.stx:4** — [4.11] No page in the repo names an `ogImage` — `grep -rn 'ogImage|og:image|twitter:image' resources/ --include='*.stx'` returns zero. welcome.stx is the one public, shareable page and has no share image, so every link to it unfurls with no card art despite marketing.stx:15 promising a summary*large*image card.
  - **Fix:** Add `ogImage: 'https://<public-origin>/images/og-image.png'` to welcome.stx's `useSeoMeta` call once the page owns its head. The asset already exists at public/images/og-image.png.
- **dist/sitemap.xml:4** — [4.4b] *(silent)* The generated sitemap emits `<loc>http://localhost/components/FeatureCard</loc>` and goes on to list `/layouts/default` (:8), `/layouts/marketing` (:13), `/partials/theme` (:18) and `/layouts/postline` (:48) as pages, alongside the five real routes — and every `<loc>` uses `http://localhost` rather than a real origin (config/app.ts:15 is `url: env.APP_URL ?? 'postline.localhost'`). It also advertises /queue, /timeline, /settings, /accounts and /analytics, all of which are auth-walled. dist/ is gitignored, but this is what `buddy build` produces and what would be deployed.
  - **Fix:** Do not ship the generated sitemap as-is. Either exclude resources/views/layouts, /partials and /components from the sitemap scan and set a real `APP_URL`, or hand-author public/sitemap.xml listing only /welcome and /login. Pair with the robots.txt above.
- **resources/views/layouts/postline.stx:8** — [4.10] `<title>@yield('title', 'Postline')</title>` — the title is interpolated into markup instead of being computed in `<script server>` and handed to `useSeoMeta`. This is a direct hit on the chapter's CHECK `grep -rn '<title>{{|<title>@' resources/views --include='*.stx'`, which must be empty. Same pattern at resources/views/layouts/marketing.stx:7. Today the rendered result is benign (one title, correct value), but it is the mechanism that makes the double-title trap in the 4.1 finding possible, and it means a title can never be derived from server data (a queued-post count, an account name) without going through the runtime head.
  - **Fix:** Remove the `<title>` element from both layouts entirely and let each page set its title via `useSeoMeta({ title })` in `<script server>`, computing any dynamic part above the call.
- **resources/views/welcome.stx:4** — [4.5b] No page emits `<link rel="canonical">`. The chapter's check expects the count of `rel: 'canonical'` declarations to equal the number of public pages; Postline's count is 0. welcome.stx is publicly indexable and has no canonical, so any query-string or trailing-slash variant of the landing page is a separate URL to a crawler.
  - **Fix:** In welcome.stx's `<script server>`, alongside `useSeoMeta`, add `useHead({ link: [{ rel: 'canonical', href: 'https://<public-origin>/welcome' }] })`. It must be `useHead`, not `useSeoMeta({ canonical })` — variable-extractor.js:323-330 (verified on 0.2.119) copies only `title` and `meta`, so the canonical key would be dropped with no error.
- **resources/views/layouts/default.stx:1** — [3.5] The 'default' layout is a 5-line passthrough — @yield('content'), @yield('footer'), @stack('scripts') — with no <main>, no chrome and no document. The chapter's CHECK (`grep -rc "<main" resources/layouts/*.stx` must be exactly 1 each) fails with 0. It is a routing group that owns no shell, which is why login.stx has had to absorb the theme bootstrap (:7-20), the dark-mode override sheet (:21-34) and the container (:36) that a real layout would own.
  - **Fix:** Give default.stx a real body: `<main>@yield('content')</main>` plus @stack('scripts'), and hoist login.stx's theme bootstrap and dark-mode CSS into it (or into config/ui.ts app.head so both shells share one copy).
- **config/ui.ts:8** — [3.6] The exported config declares only componentsDir, layoutsDir and partialsDir — there is no `app` key and therefore no app.head at all. Every piece of shared document metadata (charset, viewport, color-scheme, favicon, theme bootstrap, auth guard, fetch bridge) is instead hand-written twice, in layouts/postline.stx:3-262 and layouts/marketing.stx:3-91. The favicon <link> is literally duplicated (postline.stx:9, marketing.stx:9), as are charset and viewport.
  - **Fix:** Add `app: { head: { title: 'Postline', lang: 'en', meta: [...], link: [...], script: [...] } }` to config/ui.ts and strip the duplicated tags from both layouts. dist/document-shell.js:73-80 dedupes on re-injection, so unconditional declaration is safe.
- **resources/views/login.stx:4** — [3.6] *(silent)* `@section('description', 'Sign in to your Postline workspace.')` has no consumer. layouts/default.stx contains only @yield('content'), @yield('footer') and @stack('scripts') — no @yield('description') — and the description never reaches a <meta> because generateDocumentShell only emits metas from app.head.meta (dist/document-shell.js:20-26). The value is collapsed into context.__sections at dist/process.js:394-400 and then discarded. /login therefore ships with no meta description, silently.
  - **Fix:** Either add `<meta name="description" content="@yield('description', '…')">` to the default layout's head once it has one, or switch to `<script server>useHead({ title: '…', meta: [{ name: 'description', content: '…' }] })</script>` — the highest-precedence source at dist/process.js:258-266, valid because default.stx supplies no DOCTYPE.
- **resources/views/settings.stx:68** — [3.6] *(silent)* A 40+-line page-local `<style>` block sits loose inside @section('content') rather than behind @push('styles'). It renders wherever the section lands — inside <main> in the body (layouts/postline.stx:279-281) — not in the <head>. No layout in the repo declares @stack('styles'), so there is nowhere for it to go today.
  - **Fix:** Add `@stack('styles')` to the head of resources/layouts/postline.stx, then wrap this block in @push('styles') … @endpush.
- **resources/views/queue.stx:117** — [3.6] *(silent)* Same defect: a loose page-local `<style>` block inside @section('content'), outside any @push. Renders in the body inside <main>.
  - **Fix:** Wrap in @push('styles') … @endpush against a new @stack('styles') in the postline layout head.
- **resources/views/login.stx:21** — [3.6] *(silent)* A loose `<style>` dark-mode override block (plus a loose `<script>` theme bootstrap at :7-20) inside @section('content'), outside any @push. It duplicates roughly a third of the override layer already written in layouts/postline.stx:113-179, maintained separately.
  - **Fix:** Hoist both into the default layout (or config/ui.ts app.head) so the app and auth shells share one theme bootstrap and one override sheet; if any remainder is genuinely login-only, put it behind @push('styles').
- **resources/views/layouts/postline.stx:62** — [3.5] *(silent)* `window.STX_ROUTER_OPTIONS = { container: '[data-postline-workspace]' }` overrides the router container to the <section> at :278, so the <main> at :279 is NOT the container. Verified in 0.2.119: stx-router/dist/client.js:281-283 `shouldUseFragmentResponse()` returns `containerSel==='main'||containerSel==='[data-stx-content]'` — with a custom selector it is false, so the app surface silently opts out of the server fragment path entirely, while /login (which never sets STX*ROUTER*OPTIONS) still uses it. The server meanwhile still slices fragments by first-<main>/last-</main> (bun-plugin-stx/dist/serve.js:10171-10186), so the two halves of the contract disagree. This currently masks other problems rather than causing one, but it is an undocumented divergence from the chapter's model.
  - **Fix:** Either set `container: 'main'` and move the workspace chrome outside it, or declare the container once in config/ui.ts `router: { container: '…' }` with a comment recording that the fragment path is deliberately disabled — do not leave it as an inline <script> in the layout head.
- **config/ui.ts:10** — [6.3b] *(silent)* `componentsDir: 'resources/components'` — the exact double-prefix trap 6.3 warns about. Executed against Postline's real config with 0.2.119's loadStxConfig: root resolves to 'resources' (dist/config.js:367-369, because resources/views and resources/layouts both exist), then dist/config.js:415-417 path.joins the prefix, producing `resources/resources/components`. Lines 13 and 16 are broken identically: layoutsDir → `resources/resources/layouts`, partialsDir → `resources/resources/partials`. All three directories are dead.
  - **Fix:** Set the keys bare relative to root — `componentsDir: 'components'`, `layoutsDir: 'layouts'`, `partialsDir: 'partials'` — or delete all three and let the defaults apply. When resources/stores/ is created, add NOTHING to config/ui.ts; if storesDir is ever set it must be the bare string 'stores' (probe confirmed: 'stores' → <repo>/resources/stores, 'resources/stores' → <repo>/resources/resources/stores).
- **resources/views/layouts/postline.stx:16** — [6.5] `if (!/(?:^|;\s*)auth-token=/.test(document.cookie))` — hand-rolled cookie read in the auth-guard script. stx's own strict-mode rule reports 'document.cookie is prohibited' (verified 0.2.119 dist/script-validation.js:39).
  - **Fix:** This one is genuinely constrained — it must run pre-paint, before the signals runtime exists, so useCookie is unavailable. Keep the raw read but confine it to this single pre-paint block and delete every other document.cookie site; document the exemption in a comment as login.stx:8-11 already does for the theme bootstrap.
- **resources/views/layouts/postline.stx:28** — [6.5] `const readToken = () => document.cookie.match(/(?:^|;\s*)auth-token=([^;]+)/)?.[1]` — a second hand-rolled cookie read, this one inside the window.fetch monkey-patch that injects the Authorization header. Re-parses document.cookie on every single /api/ request.
  - **Fix:** Once resources/stores/session.ts exists, the bridge reads `window.__STX_STORES__.session.token()` — a signal read instead of a regex over document.cookie per request. Better still, drop the monkey-patch: useQuery/useMutation accept a `headers` option, so `useQuery(url, { headers: { authorization: `Bearer ${session.token()}` } })` removes the need to wrap window.fetch at all.
- **resources/views/timeline.stx:201** — [6.6] `await fetch('/api/postline/bluesky/timeline?limit=30')` behind hand-declared `busy`/`feedError`/`loaded` signals (:100-102) — the exact triad useQuery returns. No cache, so every visit to /timeline is a cold fetch; the chapter notes _queryCache lives in the runtime closure that cleanupContainer never clears.
  - **Fix:** `const feed = useQuery('/api/postline/bluesky/timeline?limit=30', { cacheKey: 'timeline', staleTime: 60000 })`, then `const rows = derived(() => (feed.data()?.items || []).map(toRow))`. Bind `feed.loading()` / `feed.error()` and delete the three state() lines; the Refresh button becomes `feed.refetch()`.
- **resources/views/queue.stx:298** — [6.6] `await fetch('/api/postline/queue')` behind hand-declared `loadError`/`loaded` (:153-154), plus a second raw fetch at :319 for publish/delete POSTs. queue.stx is otherwise the best file in the repo (9 derived, useLocalStorage, onMount) — the data layer is the only gap.
  - **Fix:** `const queue = useQuery('/api/postline/queue', { cacheKey: 'queue', staleTime: 15000 })` with `const items = derived(() => queue.data()?.items || [])`, and `const publish = useMutation('/api/postline/queue/publish-now', { onSuccess: () => queue.invalidate() })`. This also lets index.stx share the same cached queue after a create.
- **resources/views/login.stx:128** — [6.6] `await fetch(registering() ? '/api/register' : '/api/login', …)` with `const errorMessage = state('')` (:75) and `const busy = state(false)` (:76) — the chapter's WRONG(a) example, reproduced almost verbatim including the try/catch/finally shape.
  - **Fix:** `const login = useMutation('/api/login', { onSuccess: d => { session.token.set(d.access_token || d.token); location.assign('/') } })`; template binds `login.loading()` and `login.error()`; delete both state() lines. Keep a separate useMutation for /api/register.
- **resources/views/timeline.stx:195** — [6.2] refresh() performs 12 signal writes with no batch(): four consecutive at :195-198 (busy/refreshLabel/feedError/metaText), two at :206-207, three at :219-221, three at :226-228. Verified in 0.2.119 (dist/signals.js:498-503) each .set outside a batch synchronously runs every subscribed effect — so the opening four writes alone drive four separate DOM passes over the same feed header and skeleton block.
  - **Fix:** `batch(() => { busy.set(true); refreshLabel.set('Refreshing…'); feedError.set(''); metaText.set('Syncing…') })` for the prologue, and one batch each in the try/catch/finally arms. Adopting useQuery (see 6.6) removes all 12 writes instead.
- **resources/views/settings.stx:159** — [6.2] The Bluesky status promise handler writes four signals back-to-back (:159 charLimit, :160 blueskySummary, :165 blueskyPill, :166 blueskyState) with no batch, and the catch arm writes three more (:168-170). Four flushes for one server response.
  - **Fix:** Collapse to one source signal plus derived: `const status = state(null)`, `const blueskyPill = derived(() => !status() ? 'Checking' : status().canPublish ? 'Ready' : 'Setup')`, etc. One `status.set(data)` then replaces all four writes and no batch is needed — the 6.1 and 6.2 fixes are the same edit here.
- **resources/views/layouts/postline.stx:51** — [6.8] *(silent)* `window.matchMedia('(prefers-color-scheme: dark)').matches` is read exactly once, pre-paint, into a snapshot with no change listener — the chapter's hand-built WRONG for 6.8. Only /settings registers a listener (settings.stx:154, correctly via useEventListener). On /, /queue, /timeline, /analytics and /accounts an OS light↔dark flip is never reflected until a full reload. login.stx:15 has the same one-shot read.
  - **Fix:** In 0.2.119 this is a one-liner that did not exist as a global in 0.2.113: `const prefersDark = usePreferredDark()` returns a SIGNAL backed by a real matchMedia change listener with its own onDestroy (verified dist/signals.js:3852-3866, and usePreferredDark is in STX*AUTO*IMPORTS). Put it in resources/stores/theme.ts and derive isDark from it; keep the pre-paint inline read purely to avoid the first-paint flash.
- **tsconfig.json:16** — [6.11] *(silent)* `include` lists only *.ts, *.d.ts, app/Actions, app/Models, app/Services, app/Support — no path under resources/ at all — and `exclude` explicitly lists `resources/assets/**`. There is no types/ directory. Consequences: a future resources/stores/*.ts would be invisible to tsc (Bun.Transpiler erases types without checking them), and today's 403-line resources/assets/scripts/postline-analytics.ts plus the six other provider modules are never typechecked despite being .ts.
  - **Fix:** Add `"resources/stores/**/*.ts"` to include and drop `resources/assets/**` from exclude, so real `.ts` under `resources/` is typechecked.
  - **NOT the fix, and previously recommended here in error:** a hand-written `types/postline.d.ts` of ambient interfaces, plus `"types/**/*.d.ts"` in include. It was tried (a3e3f87) and reverted. Two reasons. First it does not work: `tsc` cannot see inside a `.stx` file, so ambient interfaces describing script-block locals are never checked by anything — editor hints wearing a typecheck's clothes. Three of the eight interfaces it declared were used nowhere at all, and nothing noticed. Second, each of the rest was used by exactly one view, so a global declaration file was a second source of truth for one file's internals. They now live in the `<script client>` block that uses them, which is plain TypeScript and needs no ambient file and no `include` widening. The genuine gap - that no tool typechecks `.stx` script blocks - belongs to the framework, not to an app's tsconfig.
- **resources/assets/scripts/postline-analytics.ts:343** — [6.6] *(silent)* `analyticsPromise = fetch('/api/postline/analytics')` is a hand-rolled module-level promise cache (declared :48) — useQuery's _queryCache without staleTime, cacheTime, isStale or invalidate. Once resolved it is never revalidated for the lifetime of the document, so /analytics shows stale numbers indefinitely. This module is loaded as a plain `<script type="module">` (layouts/postline.stx:292), entirely outside the stx client-script pipeline, so no auto-imports and no lifecycle are available to it at all.
  - **Fix:** Delete the module and move analytics into a `<script client>` on analytics.stx: `const a = useQuery('/api/postline/analytics', { cacheKey: 'analytics', staleTime: 60000 })`, with `derived()` for each stat tile bound via `:text=`. That also removes the hand-rolled error/retry block at :331-338 in favour of `a.error()` and `a.refetch()`.
- **resources/assets/scripts/postline-analytics.ts:394** — [6.7] *(silent)* `observer.observe(target, { childList: true, subtree: true })` — a MutationObserver over document.body's entire subtree, never disconnected, used to detect when the analytics chart element appears after an SPA swap. This is DOM-polling standing in for the reactivity the page does not have, and it runs on every mutation on every page, not just /analytics.
  - **Fix:** Once analytics.stx has a `<script client>` with `onMount(() => …)`, the observer is unnecessary — onMount fires per fragment mount by construction. If a global hook is still wanted, `useEventListener('stx:load', render, { target: window })` registers its own teardown.
- **resources/views/layouts/postline.stx:16** — [7.4] `if (!/(?:^|;\s*)auth-token=/.test(document.cookie)) window.location.replace('/login')` — the app's auth gate is a client-side cookie sniff inside a plain `<script>`. The whole authenticated shell is server-rendered and painted for a signed-out visitor before this fires. Zero `@auth`/`@guest` directives exist in the repo.
  - **Fix:** Wrap the shell in `@auth … @endauth` with a `@guest`-side redirect/notice, and populate `context.auth` from the route. processAuthDirectives (conditionals.js:208-320) resolves on the server, defaults to guest when context.auth is absent, and supports a top-level `@else` — verified working in 0.2.119. Keep the fetch bridge at :20-41; only the gate at :10-18 needs to move.
- **resources/views/accounts.stx:178** — [7.8] `form?.addEventListener('submit', async (event) => { event.preventDefault(); … })` — the Bluesky connect form is wired with a raw DOM listener whose first statement re-implements the `.prevent` modifier. Same shape at accounts.stx:637 (Mastodon form) and layouts/postline.stx:452, 461, 492.
  - **Fix:** `<form … @submit.prevent="connectBluesky">` with `function connectBluesky(e) { … }` in `<script client>`. `.prevent` fires at signals.js:2146, before the handler is invoked. login.stx:43 already does exactly this (`@submit.prevent="onSubmit"`) and its handler carries no preventDefault line.
- **resources/views/analytics.stx:16** — [7.3] *(silent)* `<div class="hidden gap-3 items-center justify-between …" data-analytics-error>` — the error banner's visibility is a `hidden` class toggled from resources/assets/scripts/postline-analytics.ts:308,323 (which also swaps a `flex` class back and forth). analytics.stx contains no script, no directive and no binding at all; the entire page is driven by that external TS file.
  - **Fix:** Move the analytics logic into a `<script client>` block in analytics.stx and render `<div class="gap-3 items-center justify-between …" style="display:none" x-show="loadError()"><span :text="loadError()">Couldn't load analytics.</span><button @click="retry" …>Retry</button></div>`. That removes both the class flip and the `flex`/`hidden` swap.
- **resources/views/index.stx:810** — [7.0.3] `if (textarea) textarea.value = item.text || ''` — composer field state is mirrored by hand (17 `.value =` writes across index.stx and accounts.stx), with the read side going back through querySelector. No `x-model` anywhere in these two files.
  - **Fix:** `const bodyText = state('')` plus `<textarea x-model="bodyText" class="…">`; bindModel (signals.js:2344) owns both directions. login.stx:51,56,60,63 uses x-model for exactly this and needs no `.value` plumbing.
- **components/PostlineSidebar.stx:105** — [9.5b] *(silent)* Same stale-state root cause as the aria-current defect, but visible: the nav icon's colour is server-rendered as `{{ link.active ? 'text-ink' : 'text-muted' }}` on a child `<span>` (also :129 for the settings icon). `updateActiveLinks()` only touches the `[data-stx-link]` anchor itself, never its descendants, and the sidebar is outside the swap container — so after an SPA navigation the icon of the page you LEFT stays `text-ink` and the icon of the page you arrived at stays `text-muted`, while the anchor's own background pill moves correctly.
  - **Fix:** Move the icon colour onto the anchor's active class set so the router manages it — e.g. `data-stx-active-class="text-ink bg-[#e8e8e6]/90 [&_[data-nav-icon]]:text-ink"` — or refresh the child span in the same `stx:navigate` handler that fixes `aria-current`.
- **resources/views/index.stx:908** — [9.7] *(silent)* `window.location.assign('/queue')` after a successful queue update. Nothing about this navigation requires a fresh document — the composer is done, the destination is a same-layout app page — so it discards the router's fragment cache, View Transitions path and progress bar for no reason.
  - **Fix:** `navigate('/queue')`. `navigate` is auto-imported on detection of `navigate(` (no import needed) and delegates to `window.stxRouter.navigate` when the router is live.
- **resources/views/login.stx:107** — [9.7] *(silent)* `location.assign('/')` in `storeTokenAndEnter`. The comment at :100-103 declares the intent ("a hard navigation rather than stx `navigate()`: the app shell reads auth state at boot"), which is a legitimate reason for a document load — but the chapter's rule is that a genuine document load is spelled `navigate(url, true)`, not a raw `location.assign`. The chapter explicitly notes bughq's own `login.stx:43` already used `navigate()`.
  - **Fix:** `navigate('/', true)` — same effect (signals.js:967-969 → `window.location.href = url`), keeps the existing justification comment, and stays lintable once `strict` is on.
- **resources/views/settings.stx:147** — [9.7] *(silent)* `location.assign('/login')` in `signOut`. Same shape as login.stx:107 — the comment at :141-144 declares the reason (drop all in-memory state), which is valid, but the raw form is what §9.7 forbids.
  - **Fix:** `navigate('/login', true)`.
- **resources/views/layouts/postline.stx:361** — [9.7] *(silent)* `button.addEventListener('click', () => window.history.back())`, and `window.history.forward()` at :366, wiring the native-shell back/forward chrome buttons. §9.7 names `goBack()`/`goForward()` as the required form.
  - **Fix:** `goBack()` / `goForward()` — both are auto-imported globals published at signals.js:5078-5079 and are literally `window.history.back()`/`.forward()` (signals.js:979-980), so behaviour is unchanged. Note the surrounding block already uses `window.stx?.useEventListener` and `window.stx.navigate` at postline.stx:486-493, so the file is inconsistent with itself.
- **resources/views/accounts.stx:276** — [9.10b] *(silent)* `const params = new URLSearchParams(window.location.search)` — a hand-rolled query read where `useRoute().query` already exists and is auto-imported. Same at accounts.stx:375 (`twitterParams`), and the instagram (:472) and threads (:568) blocks reuse the SAME mutated `params` object created at :276, after the LinkedIn block has already called `params.delete('message')` at :286 — so a callback carrying both a provider result and a `message` can read `null` for a message that was in the URL.
  - **Fix:** `const query = useRoute().query` at each read site (returns a fresh plain object each access — verified signals.js:1015-1021), then `query.linkedin`, `query.message`. This also removes the shared-mutable-state coupling between the four provider blocks.
- **resources/views/index.stx:178** — [9.10b] *(silent)* `const editParam = new URLSearchParams(window.location.search).get('edit')` — read once at script-parse time. Because it is captured eagerly and never re-read, an SPA navigation from `/queue` to `/?edit=<id>` (the exact link built at queue.stx:214 and rendered at queue.stx:51) leaves this variable holding the value from whenever the composer script last ran. Same pattern at index.stx:1313 for `?schedule=`, which is the destination of the calendar links at queue.stx:104-105.
  - **Fix:** `useRoute().query.edit` / `useRoute().query.schedule`, read inside the `start()` function rather than at module scope. `useRoute` is auto-imported; never hand-import it.
- **components/PostlineSidebar.stx:47** — [9.1] *(silent)* `<a href="/blog">` in the mobile primary nav with no `data-stx-link`, no `<StxLink>`, and no `data-no-router`. Same at PostlineSidebar.stx:110 in the desktop sidebar nav. Because `interceptAllLinks` resolves to `true` (serve.js:9788 default, and config/ui.ts sets no router block to override it), `shouldIntercept('/blog')` passes and the router intercepts, fetches, then `isStxDocument()` (client.js:399-404) decides. `/blog` has no `.stx` page — the only `/blog` route in the repo is the API endpoint at routes/api.ts:76 (`/api/postline/blog`, a JSON action) — so the response almost certainly fails the stx-document test and falls back to `location.href = url` at client.js:392, costing a wasted fetch on every click.
  - **Fix:** If /blog is a real stx page, make it `<StxLink to="/blog" activeClass="…">` like the rest of the nav. If it is served by a different engine (BunPress over content/blog), it is not one of the four §9.2 exemptions but it does need the opt-out: `<a href="/blog" data-no-router>` with the reason stated inline. Either way it must not stay as a bare `<a>`.
- **resources/views/welcome.stx:11** — [9.1] *(silent)* `<a href="/welcome" aria-label="Postline home">` — the landing-page wordmark, no `data-stx-link` and no `<StxLink>`. Same at welcome.stx:209 in the footer. Self-referential, so `shouldIntercept()` returns false at client.js:843 (`href === location.pathname`) and it does a full document reload of the page you are already on.
  - **Fix:** `<StxLink to="/welcome" class="inline-flex gap-2.5 items-center font-semibold text-ink" aria-label="Postline home">…</StxLink>` — `aria-label` passes through untouched (every static attribute not in `consumedStatic` = to/class/className/activeClass/exactActiveClass/prefetch is forwarded verbatim by stx-link.js).
- **resources/views/index.stx:252** — [9.1] `providerToggles.innerHTML = '<a class="…" href="/accounts" data-stx-link>No accounts yet. Connect one →</a>'` — a route link authored as an HTML string inside a client script. A `<StxLink>` component cannot be expressed this way at all, so this site is structurally unconvertible without restructuring. (It happens to work — the router's click listener is delegated on `document` with capture at client.js:855, so innerHTML-injected anchors are still intercepted — but it is invisible to any StxLink audit and to Crosswind class extraction.)
  - **Fix:** Render the empty state declaratively in markup with `x-show`/`@if` on the same signal that drives `providers.length === 0`, using `<StxLink to="/accounts">`, instead of assembling markup in JS.
- **resources/views/login.stx:36** — [9.9] *(silent)* login.stx is a page fragment (`@extends('layouts/default')` at :1, no `<!DOCTYPE>`) that carries its own `<main class="grid place-items-center px-4 min-h-screen bg-[#f3f3f1]" data-testid="login-page">`. The chapter's rule is that the layout owns the container and a fragment carries none. Here the layout cannot own it: resources/views/layouts/default.stx is five lines — `@yield('content')`, `@yield('footer')`, `@stack('scripts')` — with no doctype, no `<html>`, no `<head>` and no `<main>`. The chapter's own CI check flags exactly this file (`frag mains=1 resources/views/login.stx`).
  - **Fix:** Give `layouts/default.stx` a real document shell that owns a single `<main>` (or point login.stx at a proper auth layout), and delete the `<main>` from login.stx:36, moving its layout classes onto the layout's `<main>`. Note this is entangled with the layouts chapter — default.stx producing no `<html>`/`<head>` is a separate defect.
- **config/ui.ts:8** — [9.12b] *(silent)* config/ui.ts has no `router` block at all, so `interceptAllLinks` silently resolves to the serve-path hardcoded `true` (verified serve.js:9786-9790 in 0.2.119) rather than stx-router's own default of `false` (client.js:9). Meanwhile `container` and `viewTransitions` are set out-of-band by an inline `<script>window.STX_ROUTER_OPTIONS = { container: '[data-postline-workspace]', viewTransitions: false };</script>` at resources/views/layouts/postline.stx:62. Both of those keys ARE forwardable from config/ui.ts (runtime-injection.js:92, :98), so the inline script is unnecessary — and because it lives in one layout, `/welcome` (marketing) and `/login` (default) silently get `container: 'main'` instead, which is why the marketing-layout defect above bites. The inline script is also raw `window.*` in a template, which Postline's own CLAUDE.md forbids.
  - **Fix:** Move it into config/ui.ts: `router: { container: '[data-postline-workspace]', interceptAllLinks: true, viewTransitions: false }`, delete postline.stx:62, and give marketing.stx/default.stx a `<main>` so one container selector is valid across all three layouts. Then flip `interceptAllLinks: false` once §9.1 conversion is complete.
- **resources/views/layouts/postline.stx:62** — [9.12] *(silent)* Setting `container` to `'[data-postline-workspace]'` (a `<section>` wrapping the `<main>` at postline.stx:279) has an undocumented consequence the chapter does not mention because it postdates 0.2.113: `shouldUseFragmentResponse()` returns true ONLY when `containerSel` is exactly `'main'` or `'[data-stx-content]'` (verified client.js:281-283). Postline therefore NEVER receives an `X-STX-Fragment` response. Every SPA navigation downloads the complete HTML document, parses it with DOMParser, and every hover-prefetch caches a full document rather than a fragment — inflating the `prefetchCacheMax:50` LRU accordingly. The entire server-side fragment path at serve.js:10172-10226 is dead code for this app.
  - **Fix:** Set `container: 'main'` and move the workspace chrome classes so the `<main>` at postline.stx:279 IS the container (or make the `<section>` the `<main>`). That re-enables fragment responses and makes the `X-STX-Container-Attrs` round-trip coherent. Verified safe in the current shape: the document-swap branch reads `newContent = doc.querySelector(containerSel)` (client.js:527) and copies innerHTML, so the `<main>` survives today — it is a performance loss, not a correctness break.
- **resources/views/components/FeatureCard.stx:1** — [10.13] *(silent)* The file sits inside pagesDir (`resources/views`, per config.js:369), so stx routes it as a public page. Confirmed in the build: dist/components/FeatureCard.html (35 KB, fully rendered with the app layout) exists and dist/sitemap.xml contains `<loc>http://localhost/components/FeatureCard</loc>`. The same defect additionally publishes resources/views/layouts/{default,marketing,postline}.stx and resources/views/partials/theme.stx as `/layouts/default`, `/layouts/marketing`, `/layouts/postline` and `/partials/theme` — all four are in the sitemap, and dist/layouts/postline.html is a 214 KB public page.
  - **Fix:** `git rm resources/views/components/FeatureCard.stx` and remove the directory (`test ! -d resources/views/components` must pass). Move layouts and partials out of pagesDir — to `resources/layouts/` and `resources/partials/`, which is what layoutsDir/partialsDir are meant to point at once the double-prefix in config/ui.ts is fixed.
- **resources/views/components/FeatureCard.stx:2** — [10.12] *(silent)* Verbatim the chapter's WRONG example: `<script server>` declares `const title = ''`, `const description = ''`, `const image = ''` — locals masquerading as props. It never reads $props, so it always renders an empty `<h3>`, an empty `<p>`, and `<img src="" alt="">` at line 9 (a broken-image glyph). Because of the pagesDir defect above, this empty shell is a live public URL.
  - **Fix:** Delete the file (see 10.13). If a card component is wanted, author it as `resources/components/FeatureCard.stx` with `export const title = $props.title || ''` etc., and add `<slot />`, per the chapter's RIGHT block.
- **resources/views/welcome.stx:39** — [10.11] *(silent)* 27 `i-simple-icons-*` classes (github ×3, x ×5, threads/mastodon/linkedin/instagram/bluesky ×4 each) across welcome.stx lines 39, 56-61, 85-96, 117, 123, 129, 198, 216 render as nothing. Two independent causes: (a) `@iconify-json/simple-icons` is not installed — node_modules/@iconify-json contains only `f7` and `hugeicons`, and package.json declares no iconify entry at all; (b) even if installed, Crosswind's icon rule regex `/^i-([a-z][a-z0-9]*)-(.+)$/` cannot capture a hyphenated collection, so it parses collection `simple` / name `icons-github`, calls its loader for node_modules/@iconify-json/simple/icons.json, gets null, and returns undefined with no warning. Verified empirically: storage/framework/stx/cache/*.css contains generated `.i-hugeicons-*` rules and zero `.i-simple-icons-*` rules. All 25 `i-hugeicons-*` names were checked against the installed collection and every one resolves.
  - **Fix:** Replace the simple-icons spans with the stx builtin, which splits on `:` and so handles a hyphenated prefix correctly (builtins/icon.js:77-80): `bun add -d @iconify-json/simple-icons`, then `<Icon name="simple-icons:github" class="h-4 w-4" />`. The builtin also warns loudly on a missing collection and emits `<!-- Icon: … -->` on a typo, both greppable in CI (`grep -rn '<!-- Icon:' dist/ && exit 1`). Also declare @iconify-json/hugeicons in package.json — it is currently only transitively present.
- **resources/views/timeline.stx:56** — [10.8] *(silent)* `<img x-if="item.avatar" :src="item.avatar" alt="" loading="lazy" referrerpolicy="no-referrer">` inside the hand-rolled `.postline-feed-post__avatar` anchor (line 55) has no `@error` guard. The URL is a remote Bluesky CDN avatar from an untrusted third party; when it 404s or is blocked, the browser paints its broken-image glyph rather than falling back to the `item.initials` span at line 57 (the `x-else` only covers the case where `item.avatar` is falsy, not the case where it is present but dead).
  - **Fix:** Either `<Avatar :src="item.avatar" :alt="item.name" size="md" />` after §10.1 lands — Avatar.stx binds `@error="onImageError($event)"` and its `showImage` derived signal falls back to initials — or, with no config change at all, use the always-registered builtin `<SafeImage :src="item.avatar" alt="" fallback-class="…" />`, which emits `onerror="window.**stxSafeImageHandler(this)"` server-side (builtins/safe-image.js). The same `.postline-feed**skeleton-avatar` at line 38 can become `<Skeleton>`.
- **resources/views/queue.stx:352** — [10.4] `if (!confirm('Delete this post? This cannot be undone.')) return` uses the native synchronous browser confirm inside a `<script client>` block, instead of the framework's `stxConfirm`. Native confirm blocks the main thread, cannot be styled or theme-matched, is suppressible by the browser, and is the one destructive action in the queue UI.
  - **Fix:** `const ok = await window.stxConfirm('Delete this post? This cannot be undone.'); if (!ok) return` — note stxConfirm returns a Promise<boolean> (signals.js:4402), so `deleteItem` at queue.stx:351 must become `async (id) => {…}` and the `void runAction(...)` call awaited or kept as-is inside the async body.
- **resources/views/index.stx:515** — [10.12] *(silent)* `postTemplate(number, text)` is a JS template literal that re-types the composer-post markup already written as static HTML at index.stx:65-77, and the two have already diverged: the static article is `pb-3`, the JS clone at line 531 sets `pb-4`; the static remove button (line 71) is `class="hidden items-center justify-center h-7 w-7 …"`, the JS one (line 521) is `class="inline-flex items-center justify-center h-7 w-7 …"`; the static select-post button (line 67) is `bg-accent text-on-accent`, the JS one (line 517) is `bg-panel-3 text-muted`; and the static textarea carries `placeholder="What do you want to share?"` while the JS one (line 525) has none. It is injected via `document.createElement` + `innerHTML` at lines 525-531. This is the exact duplication §10.7 exists to eliminate ("factor one local component instead"), and it also breaches CLAUDE.md's no-vanilla-JS-in-stx-templates rule.
  - **Fix:** Extract one `resources/components/ComposerPost.stx` taking `:number` and `:text` props with `<script server>`/`<script client>`, render the initial post with it, and drive the list with an `x-for` over a `state([])` array of posts rather than `createElement`/`innerHTML`. Note this is an inference from §10.7's worked example and §10.12's authoring rule — the chapter does not name this file.
- **resources/views/welcome.stx:8** — [12.5] *(silent)* 13 `<!-- -->` section-marker comments (:8, :22, :28, :31, :45, :75, :103, :106, :135, :149, :164, :189, :206) — the single largest concentration in the tree. None carries a banned token today, so nothing breaks, but all 13 ship verbatim to every marketing-page visitor and each is one word away from suppressing the document shell for a page that is the app's front door.
  - **Fix:** Convert all 13 to `{{-- … --}}`.
- **resources/views/timeline.stx:32** — [12.5] *(silent)* Two further `<!-- -->` comments on active app pages beyond the backtick ones already listed: timeline.stx:32 (x-show placement) and timeline.stx:53 (@bind:style vs :style).
  - **Fix:** Convert to `{{-- … --}}`.
- **resources/views/settings.stx:24** — [12.5] *(silent)* `<!-- -->` comment explaining the data-attribute active state. Ships to the browser.
  - **Fix:** Convert to `{{-- … --}}`.
- **resources/views/accounts.stx:8** — [12.5] *(silent)* `<!-- -->` comment documenting the Crosswind safelist for connection status pills. Ships to the browser.
  - **Fix:** Convert to `{{-- … --}}`.
- **resources/views/index.stx:49** — [12.5] *(silent)* `<!-- -->` comment about per-network override rows. Ships to the browser.
  - **Fix:** Convert to `{{-- … --}}`.
- **resources/views/layouts/marketing.stx:11** — [12.5] *(silent)* Two `<!-- -->` comments in a layout (:11 Open Graph, :17 font loading). Layouts are explicitly in 12.5's prohibited scope, and a comment in a layout ships on every page that extends it.
  - **Fix:** Convert both to `{{-- … --}}`.
- **package.json:81** — [12.8] *(silent)* The bun-git-hooks staged-lint glob is `*.{js,ts,json,yaml,yml,md}` — `stx` is absent, so none of Postline's 31 .stx files are ever linted on commit, even though config/code-style.ts:5 lists `stx` in format.extensions and pickier 0.1.44 does emit pickier/sort-tailwind-classes on .stx (control-tested).
  - **Fix:** Change the glob to `"*.{js,ts,stx,json,yaml,yml,md}"`.
- **resources/views/layouts/postline.stx:63** — [12.9-g1] A 196-line `<style>` block (:63-259) in the shell layout — hand-written CSS on every app page, invisible to pickier's sort-tailwind-classes rule and outside Crosswind's scan. Four more: views/layouts/marketing.stx:22-90 (68 lines), views/queue.stx:117-146, views/settings.stx:68-88, views/login.stx:21-35. Total 5 blocks, ~260 lines.
  - **Fix:** Move the rules into Crosswind utility classes on the elements, or into config/crosswind.ts as custom rules/theme tokens if they are genuinely reusable primitives. Anything that must stay CSS belongs in resources/assets/styles/, not inline in a .stx file — Postline already has postline-timeline.css and postline-toast.css there as precedent.
- **resources/views/index.stx:16** — [12.9-g2] 76 inline `style="` attributes across resources. On the active pages: index.stx:16 and :677, login.stx:49/:61/:65, queue.stx:84. The recurring shape is `style="display:none"` paired with `x-show`, which the surrounding comments defend as the pre-hydration state. One reported hit — timeline.stx:55 — is a regex false positive: it is `@bind:style="item.gradient"`, the stx-native binding.
  - **Fix:** For the `display:none` + `x-show` pairs, use `x-cloak` — stx injects `[x-cloak]{display:none!important}` into the page itself (dist/process.js runtime preamble), which is exactly the pre-paint hiding these comments describe, without an inline attribute. index.stx:677 builds a style string inside a template literal, so it goes away with the createElement/x-for rewrite under 12.9-step4. Leave timeline.stx:55 alone.
- **resources/views/index.stx:593** — [12.9-g4] Two unmanaged timers in the composer: `draftSaveTimer = setTimeout(saveDraft, 100)` at :593 (paired with clearTimeout at :592) and `previewTimer = setTimeout(…)` at :719 (paired with clearTimeout at :718). Both are hand-rolled debounces that survive a fragment swap.
  - **Fix:** Both are textbook useDebounce() — replace the clearTimeout/setTimeout pairs with `const saveDraftDebounced = useDebounce(saveDraft, 100)` and the equivalent for the preview, which stx tears down on unmount.
- **.claude/skills/stacks-browse/SKILL.md:16** — [12.7a] The chapter mandates .claude/skills/stacks-browse/scripts/{spa-probe.ts,spa-shot.ts,link-intercept.ts} as the only acceptable proof of SPA navigation. Postline's stacks-browse skill ships a single driver, browse.ts, whose commands are navigate/screenshot/responsive/monitor/snapshot (browse.ts:392, :409, :422, :441, :459). None of the three probes exists anywhere in the repo. Every SPA rule in 12.7 and checklist items 9-12 are therefore unverifiable in this project, not merely unverified.
  - **Fix:** Port the three scripts into .claude/skills/stacks-browse/scripts/ (or app/Skills/), then run the 12.7 sequence against Postline's actual routes: `spa-probe.ts routes <base> / /queue /timeline /settings /accounts /analytics /login`, `spa-probe.ts navs <base> /::/queue /queue::/timeline …`. Also add a `router` key to config/ui.ts — the chapter assumes `config/ui.ts:router` exists and Postline has none, so it is currently unclear whether the SPA router is configured at all (stx-router is installed at node_modules/stx-router, and pages do emit the `stx-layout` meta that router/client.js isStxDocument looks for).
- **resources/views/analytics.stx:53** — [11.2] *(silent)* The analytics chart hard-codes its palette as SVG presentation attributes (`fill="#fafafa"` at :53, `stroke="#2563eb"` at :60, plus more emitted from resources/assets/scripts/postline-analytics.ts:145,167,169), which then forces a five-rule dark-mode patch in the layout stylesheet at resources/views/layouts/postline.stx:175-179 that targets those literal hex values via attribute selectors (`rect[rx="16"]`, `line[stroke="#e4e4e7"]`, `text[fill="#18181b"]`). Change any hex in the chart and the dark theme silently stops matching.
  - **Fix:** Give the chart var-backed fills — `fill="var(--panel-2)"`, `stroke="var(--accent)"` — so it flips with the token swap, and delete postline.stx:175-179 entirely.
- **app/Services/Social/BlueskyService.ts:9** — [11.10] `SocialIdentityRow` is declared six times and `SocialDriverRow` six times, once each in BlueskyService.ts:9/:22, TwitterService.ts:9/:23, MastodonService.ts:9/:21, ThreadsService.ts:9/:23, InstagramService.ts:9/:23, LinkedInService.ts:9/:23. The copies are field-for-field identical apart from the `provider` string literal (and Twitter's extra token*expires*at). This is the chapter's duplicate-name CHECK firing: `grep -rhoE '^(export )?(interface|type) [A-Z]...' | sort | uniq -d` prints exactly SocialDriverRow and SocialIdentityRow.
  - **Fix:** Hoist to a single generic declaration in the shared module that already exists — app/Support/Social/types.ts — e.g. `export type SocialIdentityRow<P extends ProviderKey = ProviderKey> = { ...; provider: P; ... }` — and import it in all six services. The chapter prescribes types/domain.ts; app/Support/Social/types.ts is the equivalent single home Postline already has.
- **tsconfig.json:12** — [11.10] There is no `types/` directory in the repo at all, so the chapter's prescribed home for shared app types (types/domain.ts, types/api.ts, types/views.ts, types/props.ts, types/globals.d.ts) does not exist. `~/*` already maps to `./*` (tsconfig.json:12), so the import path would work today.
  - **Fix:**Create types/ with domain.ts / api.ts / views.ts / props.ts and the globals.d.ts from rule 11.12c, and add "types/**/*.d.ts" plus "types/**/*.ts" to `include`.
- **resources/views/index.stx:531** — [11.4] *(silent)* `post.className = 'grid gap-2 pb-4 last:pb-0 border-b border-line last:border-b-0'`. The literal `pb-4` appears nowhere in a class= attribute in the repo (verified), so no `.pb-4` rule is generated. Every composer thread post created at runtime loses its bottom padding; the other five utilities in that string do happen to appear in static markup elsewhere on the page, so only pb-4 breaks.
  - **Fix:** Add pb-4 to `safelist` in config/crosswind.ts, or add it to the static <template> markup the composer clones from.
- **resources/views/index.stx:16** — [11.4] *(silent)* A hidden <div> stuffed with 40 utility class names and `style="display:none"`, existing solely to make Crosswind emit rules for classes applied from script. The comment at :8-15 documents the workaround. This is the chapter's `safelist` requirement re-implemented in markup, and it is already incomplete (it misses pb-4, block, rotate-180, opacity-0).
  - **Fix:** Move every class in this div into `safelist: [...]` in config/crosswind.ts and delete the div. The safelist is merged at dev-server/crosswind.js:301-304 and force-generated at :314-315 — that is exactly what this div is emulating, minus the drift.
- **resources/views/queue.stx:117** — [11.1] 30 lines of CSS (117-146) inside the page: [data-view-toggle], [data-status-pill] × 4 statuses × light+dark, [data-cal-dot], [data-cal-cell], [data-cal-day]. It consumes var(--muted)/var(--panel)/var(--panel-3)/var(--line)/var(--panel-2) supplied by the layout, and hard-codes 20 rgb() literals for the status palette.
  - **Fix:** Register status colours as tokens in theme.extend.colors (status-published, status-scheduled, status-publishing, status-failed, each with surface/border/text) and drive the pills with utilities; keep the attribute-selector approach only in `preflights` if the data-attribute binding must stay.
- **resources/views/settings.stx:68** — [11.1] 21 lines of CSS (68-88) inside the page: [data-theme-option] active state and [data-bluesky-pill] ready/setup states, in both light and html.dark variants, with 6 hard-coded rgb() literals.
  - **Fix:** Same as queue.stx — status/state colours become tokens in theme.extend.colors; anything that must remain attribute-driven goes to `preflights` in config/crosswind.ts.
- **resources/views/login.stx:21** — [11.1] 15 lines of CSS (21-35) that are a pure `.dark .utility {}` override layer — .dark .bg-white, .dark .border-zinc-200, .dark .text-neutral-950, .dark .hover\:bg-neutral-800, etc. — exactly the layer the semantic-token system exists to eliminate. login.stx extends layouts/default (which is a bare @yield with no <head> and no tokens), so it is the one page that cannot use bg-panel/text-ink and instead re-implements dark mode by hand.
  - **Fix:** Give login a real layout that emits the token preflight (or move the preflight into config/crosswind.ts per rule 11.2, at which point every page gets it), then rewrite the markup with bg-panel / border-line / text-ink / bg-accent and delete the entire <style> block.
- **resources/views/login.stx:49** — [11.5] `style="display:none"` used as a pre-hydration hider on x-show elements — three occurrences at :49, :61 and :65. Also resources/views/queue.stx:84 and resources/views/index.stx:16. `display:none` is a static utility (`hidden`), not a per-render unbounded value, so it is squarely inside what the chapter forbids.
  - **Fix:** Use `class="hidden"` and safelist `hidden` (it is already in static markup on every one of these pages, so no safelist entry is even needed). If the concern is that a generated `.hidden` could be out-ordered by a layout utility on the same element — the reason given in the index.stx:8-15 comment — fix the ordering by not putting competing display utilities on the same element, rather than by escalating to inline style.
- **resources/views/index.stx:677** — [11.5] `style="background-image:url('${escapeHtml(preview.image)}')"` string-concatenated into an innerHTML template literal. The value itself is legitimately per-render and unbounded (a link-preview image URL), which the chapter allows — but it explicitly requires such values be bound with `:style`, never concatenated into a static attribute.
  - **Fix:** Render the preview card as stx markup with `@bind:style` (the pattern already used correctly at resources/views/timeline.stx:55) instead of assembling HTML strings in an inline <script>.
- **resources/views/index.stx:172** — [11.11] *(silent)* The chapter's own CHECK grep fires here: `let providers = []` at :172 and `let editingId = null` at :179, both unannotated. These are in an inline <script> block, which shouldTranspileTypeScript treats as TypeScript (utils.js:31-33), so annotations are legal and erased — there is no excuse. The wider problem is that this 1,200-line client script and the client blocks in queue.stx, settings.stx, timeline.stx and login.stx are entirely untyped (every callback parameter is implicit any: settings.stx:120,127; timeline.stx:114,121,134,152,170; queue.stx:163,167,172,178,186,197,351).
  - **Fix:** Hoist the shared shapes (QueueRow, TimelineRow, ProviderStatus, ...) into types/domain.ts, annotate the bindings (`let providers: ProviderStatus[] = []`, `let editingId: number | null = null`) and every callback parameter. Note this is only *checked* once rule 11.9 lands — tsc never opens .stx files, so the real durable fix is to move these scripts into typed .ts modules under resources/assets/scripts/ AND remove resources/assets/** from `exclude`.

### low (71)

- **resources/components/Taskbar.stx:19** — [8.4] *(silent)* 18 DOM-0 `onclick=` attributes across the leftover desktop-shell demo: Taskbar.stx:19,40,41,42; Window.stx:35,36,37; LoginForm.stx:27,29,40; StartMenu.stx:48,52; StartMenuItem.stx:25,33; NotificationPopup.stx:24,41; LoginScreen.stx:24; DesktopIcon.stx:41. StartMenu.stx:52 is `onclick="window.location.reload()"`, which is both a DOM-0 handler and a raw navigation. These files are unreferenced by any active page.
  - **Fix:** These are dead scaffolding — `resources/layouts/Desktop.stx` is referenced by nothing and the components exist only to feed it. Delete the directory. If any is kept, convert to `@click="handler"` so the handler can reach the page's signals, which a DOM-0 attribute cannot.
- **resources/partials/welcome-pdf-toolbar.stx:2** — [8.4] *(silent)* 5 DOM-0 handlers (`onclick="printWelcome()"` at 2, `onclick="downloadWelcome()"` at 3, `onclick="decreaseZoom()"` at 7, `onchange="setZoomFromInput()"` at 8, `onclick="increaseZoom()"` at 9) calling functions that are not defined anywhere in the repo, on a partial that is not @included by any view.
  - **Fix:** Dead file — delete resources/partials/welcome-pdf-toolbar.stx and welcome-pdf-content.stx. The handlers reference undefined globals, so these buttons throw on click if the partial is ever rendered.
- **resources/views/partials/theme.stx:3** — [8.5] *(silent)* A 7-line orphan theme bootstrap with `localStorage.getItem('theme')` at 3, `document.documentElement.setAttribute('data-theme', t)` at 4 and `.classList.toggle('dark', ...)` at 5. It is @included by nothing; the live copies of this logic are duplicated at resources/views/layouts/postline.stx:44-61 and resources/views/login.stx:12-19.
  - **Fix:** Delete this file, or make it the single home the other two copies @include. The chapter's 8.11 prescription applies directly: consolidate the pre-paint bootstrap into ONE partial so the strict report names exactly one file and any second file appearing is a real regression. Note the orphan also defaults to 'light' while the live copies default to 'system', so it is drifted as well as dead.
- **resources/components/Window.stx:2** — [5.11] *(silent)* `import { defineProps, withDefaults } from 'stx'` — the bare specifier `stx` does not resolve (no node_modules/stx; package.json:65 maps "stx" to the ./buddy binary, not a module). convertToCommonJS rewrites it to `const { defineProps, withDefaults } = await import('stx')`, which throws ResolveMessage, so the entire server block never executes. Reproduced with the shipped 0.2.119 extractVariables: STX_DEBUG=1 prints `server <script> did not execute in resources/components/Window.stx — Cannot find package 'stx'` and the resulting context has zero keys. Both names are already injected into the server scope with no import (variable-extractor.js:466-470).
  - **Fix:** Delete line 2. `defineProps` and `withDefaults` are supplied by the Function scope stx builds at variable-extractor.js:466-470; importing them is unnecessary and fatal.
- **resources/layouts/Desktop.stx:2** — [5.11] *(silent)* Same unresolvable `import { defineProps, withDefaults } from 'stx'`. Verified: context keys after extractVariables = []. Consequence is visible here — line 50 is `<script type="module" src="{{ scriptPath }}">`, and with the block dead `scriptPath` never enters the context, so the module src renders as the literal `{{ scriptPath }}` (cloaked by addCloakToUnresolvedExpressions, process.js:665) and 404s.
  - **Fix:** Delete line 2.
- **resources/components/DesktopIcon.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes, so the 10 `{{ }}` interpolations in this file (including data-icon-id/type/section/url at :35-38) resolve only for props the caller passes explicitly — every withDefaults default is lost.
  - **Fix:** Delete line 2.
- **resources/components/StartMenuItem.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (8 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/ContextMenuItem.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (5 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/InputGroup.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (5 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/LoginForm.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (5 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/LoginScreen.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (3 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/StartMenu.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (3 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/PackageItem.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (3 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/FeatureButton.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (2 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/NotificationPopup.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (2 interpolations affected).
  - **Fix:** Delete line 2.
- **resources/components/ContextMenu.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes (1 interpolation affected).
  - **Fix:** Delete line 2.
- **resources/components/Taskbar.stx:2** — [5.11] *(silent)* Same unresolvable 'stx' import; server block never executes, so the `showStartMenu: true` default from withDefaults is never applied.
  - **Fix:** Delete line 2.
- **resources/views/login.stx:36** — [2.8] *(silent)* `<main class="grid place-items-center …" data-testid="login-page">` is declared by the page. The chapter's literal CHECK (`grep -lE '^[[:space:]]*<main[ >]'` over non-layout views) returns this file. MITIGATING: `layouts/default.stx` is a bare three-line fragment with no `<main>`, so the 'exactly one <main> per rendered page' invariant still holds and neither the server fragment extractor (bun-plugin-stx/dist/serve.js:10171-10186) nor extractContainerContent can pick the wrong span. This is a violation in letter with no live failure — hence low.
  - **Fix:** Move `<main>` into resources/views/layouts/default.stx as `<main>@yield('content')</main>` and change login.stx:36 to a `<div>` with the same classes, so login matches the ownership model the six postline pages already follow (postline.stx:279 owns the only <main>).
- **resources/layouts/Desktop.stx:20** — [2.2] *(silent)* Leftover desktop-shell demo layout, a full document: `<!DOCTYPE html>` (20), `<html lang="en">` (21), `</body></html>` (52-53), plus hand-written `<title>{{ title }}</title>` (25) and four head `<link>` tags (26-29). Nothing references it — no route, no @extends, no @include anywhere in resources/, routes/ or app/. Its 13 companion components in resources/components/*.stx are equally unreferenced (the dev server hardcodes componentsDir to the framework defaults at views.js:71, so they are not even reachable as components). Flagged for completeness only.
  - **Fix:** Delete resources/layouts/Desktop.stx and resources/components/*.stx along with resources/assets/scripts/main.ts (which itself embeds `<!DOCTYPE html>` at :1880 and :1912 inside template literals). If the demo is worth keeping, rewrite Desktop.stx as a fragment layout with `@head … @endhead` before anything extends it.
- **resources/views/welcome.stx:8** — [2.3a] *(silent)* This is the non-binding 'prefer' half of §2.3, not the MUST. 21 HTML comments across six active views (welcome.stx 13 — lines 8, 22, 28, 31, 45, 75, 103, 106, 135, 149, 164, 189, 206; timeline.stx 3 at 32, 53, 74; index.stx 2 at 8, 49; settings.stx 1 at 24; login.stx 1 at 45; accounts.stx 1 at 8), plus 2 in marketing.stx (11, 17). Zero `{{-- --}}` comments exist anywhere in resources/views. None currently contains `<!DOCTYPE` or `<html>`, so the hard rule is clean — but the trap is live: hasDocumentShell's `/<html[\s>]/i` is unanchored and runs after comments are unmasked, so one explanatory comment mentioning `<html>` would silently kill shell generation for that page.
  - **Fix:** Convert all `<!-- … -->` in resources/views to `{{-- … --}}`, which is stripped before any gate reads the template. The comments in these files are architectural notes for maintainers, not browser-facing content, so nothing is lost.
- **resources/views/partials/theme.stx:1** — [2.4] *(silent)* Surfaced by the chapter's §2.4 CHECK (which excludes */layouts/* and */components/* but NOT */partials/*): this file declares no @extends, no @layout and no @nolayout. It is NOT a real breach — bun-plugin-stx/dist/serve.js:9298 route-excludes ['layouts','components','partials'], so it is never served as a page. It is, however, dead code: nothing @includes it, and the effective partialsDir is `resources/partials` (views.js:63-67 firstExistingPath picks it first), not `resources/views/partials`. Its inline theme bootstrap is superseded by layouts/postline.stx:44-61 and login.stx:12-19.
  - **Fix:** Delete resources/views/partials/theme.stx. If a shared theme bootstrap is wanted, move it to resources/partials/ (the directory the server actually resolves) and @include it from the layouts.
- **resources/layouts/Desktop.stx:1** — [1.6c] *(silent)* resources/layouts/ contains exactly one file, Desktop.stx — a leftover desktop-shell demo with zero references anywhere in resources/views, routes/ or app/. There is no resources/layouts/default.stx, so the chapter's check `test -e resources/layouts/default.stx` fails. Under Loader A the configured layoutsDir is resources/resources/layouts (missing), so process.js:387-390 finds no `${defaultLayout}.stx` and applies no layout, silently. This does not bite today only because every page uses an explicit @extends (verified across all eight views), but this one demo file is simultaneously what forces resolveStxRoot to return root: 'resources' (rule 1.1).
  - **Fix:** Delete resources/layouts/Desktop.stx together with the 13 unreferenced resources/components/*.stx desktop-demo files, then land the 1.4 moves so resources/layouts/ holds default.stx, marketing.stx and postline.stx.
- **resources/views/welcome.stx:1** — [4.9b] Postline's public product landing page carries no JSON-LD. `grep -rn 'ld+json|structuredData|schema.org' resources/ config/ public/` returns zero across the repo. The chapter's grep-able rules pass (nothing hand-written, no breadcrumb navs to pair) but the manual-coverage requirement — a product/app landing page needs `SoftwareApplication`, and the project identity needs `Organization` — is unmet.
  - **Fix:** Add `@structuredData({ '@type': 'SoftwareApplication', name: 'Postline', applicationCategory: 'SocialNetworkingApplication', operatingSystem: 'Web', offers: { '@type': 'Offer', price: '0' } })` above the `<main>` in welcome.stx. Use the expression form only — the `@structuredData … @endstructuredData` block form is still dead in 0.2.119 (registerSeoDirectives at seo.js:340 is called nowhere in dist) and would render as literal text into the body.
- **resources/layouts/Desktop.stx:20** — [4.1] Leftover desktop-shell demo. Hand-writes `<!DOCTYPE html>` (:20), `<html lang="en">` (:21), `<head>` (:22), `<title>{{ title }}</title>` (:25) and `</head>` (:31) — violating 4.1 and 4.10 simultaneously. Not routed and not extended by any active view, so no user-visible impact; flagged for completeness and because it is a template someone may copy. Note: resources/emails/subscription-confirmation.stx:10-17 also matches the 4.1 grep but is NOT a violation — an email template legitimately is a standalone HTML document, not a page.
  - **Fix:** Delete resources/layouts/Desktop.stx along with the rest of the resources/components/*.stx desktop demo, or if it is kept as a reference, strip the shell and let config/ui.ts own it like any other layout.
- **resources/views/layouts/default.stx:3** — [3.11] *(silent)* `@yield('footer')` is never filled — no page in resources/views declares @section('footer'). dist/process.js:423-425 substitutes '' with no warning. Confirmed by the chapter's comm check adapted to Postline's paths, which prints exactly two names: footer, scripts.
  - **Fix:** Delete the line. If the auth shell needs a footer, put the markup directly in the layout.
- **resources/views/layouts/marketing.stx:94** — [3.11] *(silent)* `@yield('footer')` is never filled — welcome.stx renders its footer inline inside @section('content') (welcome.stx:205-219) instead.
  - **Fix:** Delete the line, or move welcome.stx's inline footer markup into the layout so all marketing pages share it.
- **resources/views/layouts/postline.stx:509** — [3.11] *(silent)* `@stack('scripts')` is never pushed to — zero @push('scripts') exist anywhere in resources/. processStackReplacements (dist/includes.js:729) renders it as ''. The same dead slot is repeated at layouts/default.stx:5 and layouts/marketing.stx:95, so all three active layouts carry an unfilled stack.
  - **Fix:** Delete all three @stack('scripts') lines, or start using @push('scripts') from the pages that currently inline <script type="module"> tags into the layout (postline.stx:286-292).
- **resources/layouts/Desktop.stx:20** — [3.1] Leftover desktop-shell demo layout ships `<!DOCTYPE html>` (:20) and `<html lang="en">` (:21). It is referenced by zero pages in resources/views. Flagged per the chapter's repo-wide CHECK; low priority per the audit's stated ordering.
  - **Fix:** Delete resources/layouts/Desktop.stx together with the 13 unreferenced resources/components/*.stx demo files, or strip the DOCTYPE/<html>/<head> if the demo is kept.
- **resources/layouts/Desktop.stx:30** — [3.11] *(silent)* Desktop.stx declares 9 @yield slots (login-screens :33, desktop-icons :37, sponsor-icon :40, trash-icon :41, windows :42, taskbar :44, context-menus :47, notification-popup :48) plus @stack('head') :30 and @stack('scripts') :51 — none of which any page fills. Every one renders as ''. Leftover demo; low priority.
  - **Fix:** Delete the file along with the desktop demo components.
- **resources/emails/subscription-confirmation.stx:10** — [3.1] Contains `<!DOCTYPE html>` (:10) and `<html lang="en">` (:11). This is a literal hit for the chapter's CHECK (`grep -rn "DOCTYPE\|<html[ >]" resources --include="*.stx"`) but the mechanism does NOT apply: resources/emails is not in the dev server's `patterns` (@stacksjs/actions/dist/dev/views.js:69 passes only resources/views and the framework defaults), it produces no dist/ output, and an email template legitimately requires a complete standalone document. Reported for completeness so the check's output is fully accounted for; I do not recommend changing it.
  - **Fix:** No change. If the repo adopts the chapter's CHECK verbatim in CI, scope it to `resources/views resources/layouts resources/partials resources/components` rather than all of `resources`.
- **resources/views/queue.stx:152** — [6.11] *(silent)* `const items = state([])` — the page's central fact, whose entire shape (item.status, item.providers[].remoteUri, item.providers[].failureReason, item.hasImage, item.hasLink, item.scheduledAt, item.publishedAt, item.title) is implied only by toRow() 45 lines below. Same class as the chapter's `state(null)` example; the literal `grep 'state(null)\|state<any>'` returns nothing only because the untyped initialiser here is `[]`. timeline.stx:97 is identical.
  - **Fix:** `const items = state<QueueItem[]>([])` against the global interface above, and `useQuery<QueueResponse>('/api/postline/queue', …)` once 6.6 is applied.
- **resources/assets/scripts/site-mode.js:17** — [6.5] Three raw document.cookie operations (:17 delete, :22 write with hand-typed `Path=/; Max-Age=2592000; SameSite=Lax`, :29 read) — the exact serialisation string useCookie generates, retyped. Note this file also omits Secure, which useCookie derives from location.protocol automatically.
  - **Fix:** Only reachable from the leftover Desktop demo. Delete the file with the rest of the demo; if kept, move the cookie into resources/stores/session.ts via useCookie.
- **resources/assets/scripts/main.ts:232** — [6.7] *(silent)* `setInterval(updateClock, 1000)` at module scope, never cleared, plus 13 bare setTimeout calls (:92, :109, :608, :694, :720, :811, :828, :934, :1783, :1898, :2003, :2015, :2130). Loaded only by resources/layouts/Desktop.stx:50, which nothing extends (all seven active views extend layouts/postline, layouts/default or layouts/marketing).
  - **Fix:** Dead code for the shipping app — delete resources/layouts/Desktop.stx, resources/components/*.stx (13 files, all `<script server>` only) and resources/assets/scripts/main.ts together. If any of it is retained, useInterval (dist/signals.js:3552) pairs its timer with onDestroy(pause).
- **resources/views/queue.stx:38** — [7.1] *(silent)* `<p class="m-0 p-3 text-[13px] text-muted" x-show="showPendingEmpty()">Nothing queued yet. …</p>` — `showPendingEmpty` is a derived that is false until `loaded()` flips, i.e. the SSR state is hidden, but the tag carries no `style="display:none"` counterpart. Same at queue.stx:37 (loadError) and queue.stx:65 (showRecentEmpty). The calendar section at queue.stx:84 in this same file does carry the counterpart, so the file is internally inconsistent.
  - **Fix:** Add `style="display:none"` to the three tags, matching queue.stx:84 and login.stx:50,61,65. Note that 0.2.119 masks the flash on its own (see version drift): addCloakToConditionalDirectives now auto-stamps x-cloak on any x-show tag. Under the 0.2.113 the chapter was written against, this would flash "Nothing queued yet" on every load.
- **resources/views/timeline.stx:35** — [7.1] *(silent)* `<div x-show="busy()">` wrapping the skeleton rows — `busy` is `state(false)` (timeline.stx:100), so the SSR state is hidden, but there is no `style="display:none"` counterpart. Same at timeline.stx:48 (feedError) and :49 (showEmpty, whose fallback text reads "No posts in your feed yet").
  - **Fix:** Add `style="display:none"` to all three. The comment at timeline.stx:32-34 shows the author already reasoned carefully about bindFor/x-show interaction — this is the one counterpart that was missed.
- **resources/views/queue.stx:52** — [7.1] *(silent)* `<button … x-show="row.actionable" :disabled="busyId() === row.id" @click="publishItem(row.id)">Publish now</button>` — `:disabled` with no static `disabled` attribute. Also at queue.stx:53. (login.stx:66 and timeline.stx:16 look like the same shape to the chapter's grep but are correct: their SSR state is enabled, so no static `disabled` is the right counterpart.)
  - **Fix:** Add a bare `disabled` attribute if a freshly cloned row should start non-interactive; otherwise this is defensible because the tag lives inside an `x-for` template that bindFor clones and binds before insertion. Worth a one-line comment either way, since the chapter's check flags it.
- **resources/components/Taskbar.stx:19** — [7.7] `<button class="start-button" onclick="toggleStartMenu()">` — DOM-0 event attribute. 17 more across the leftover desktop demo: LoginForm.stx:27,29,40; Taskbar.stx:40,41,42; StartMenu.stx:48,52; NotificationPopup.stx:24,41; Window.stx:35,36,37; LoginScreen.stx:24; StartMenuItem.stx:25,33 (inside the malformed @if blocks).
  - **Fix:** `@click="toggleStartMenu"` with the function declared in `<script client>`; parseEventShorthand (signals.js:1352-1361) invokes a bare identifier with the event. These files are an unreferenced desktop-shell demo — nothing extends resources/layouts/Desktop.stx — so deleting the directory is the cheaper fix.
- **resources/partials/welcome-pdf-toolbar.stx:2** — [7.7] `<button onclick="printWelcome()" …>` plus onclick at :3, :7, :9 and `onchange="setZoomFromInput()"` at :8. The handlers live in resources/assets/scripts/main.ts, i.e. global scope reachable only because these are DOM-0 attributes.
  - **Fix:** `@click="printWelcome"` / `@change="setZoomFromInput"` with the functions in a `<script client>` block in the partial. This partial has no consumer in any .stx file, so deletion is also valid.
- **resources/components/FeatureButton.stx:20** — [7.0] *(silent)* `class="(active)active@endif @if feature-btn"` — a bare `@if` with no condition and an orphan `@endif` inside a class attribute. Verified by rendering through processConditionals in the installed 0.2.119: the string passes through untouched and ships to the browser as `class="(active)active@endif @if feature-btn"`, so the element gets literal `@endif` and `@if` as CSS class names. Identical breakage at StartMenuItem.stx:24, :32, :40.
  - **Fix:** `class="feature-btn" :class="active ? 'active' : ''"` — or, given these files are dead demo code with no consumer, delete resources/components/ and resources/layouts/Desktop.stx.
- **resources/partials/welcome-pdf-content.stx:19** — [7.0.6] `const gettingStarted = [{ step: '1', … }, { step: '2', … }, { step: '3', … }]`, printed at :73 as `{{ step.step }}` — the ordinal is hand-carried in the data even though this is a plain `@foreach` where `loop.iteration` is in scope.
  - **Fix:** Drop the `step` key and print `{{ loop.iteration }}`. Verified working in 0.2.119: processLoops injects loop/$loop per iteration (loops.js:424-425). This partial has no .stx consumer, so deletion is equally valid.
- **tsconfig.json:16** — [9.11b] *(silent)* `include` is `["*.ts", "app/Actions/**/*.ts", "*.d.ts", "app/Models/**/*.ts", "app/Services/**/*.ts", "app/Support/**/*.ts"]` — no `".stx/*.d.ts"`. The chapter's check `grep -q '".stx/\*.d.ts"' tsconfig.json || echo 'FAIL'` fails. `.stx` is correctly gitignored (.gitignore:51), so that half adheres, and no file value-imports `stx/routes` anywhere, and there is no hand-written route union — the only gap is that the generated `RouteMap` is invisible to tsc.
  - **Fix:** Add `".stx/*.d.ts"` to tsconfig.json `include`. Note Postline has no `types/` directory yet and no dynamic `[param]` routes, so the `types/routes.d.ts` re-export the chapter prescribes has nothing to type today — the tsconfig entry is worth landing now so it is in place when the first dynamic route appears.
- **components/PostlineSidebar.stx:3** — [9.10a] The chapter's §9.10 CHECK grep (`requestContext.url()`) hits here: the `<script server>` block does `requestContext.url()` at :3, `new URL(requestUrl).pathname` at :8, inside a try/catch that falls back to `'/'`, with a hardcoded `'http://localhost/'` default at :4. Being precise about scope: this is NOT the violation §9.10 describes — it reconstructs the current PATHNAME for active-nav state, not a route param, and no dynamic `[param]` route exists in Postline at all. The chapter gives no server-side alternative for reading the current pathname, and §9.5 explicitly requires server-rendering the authoritative active state, so this is arguably mandated by §9.5. Reporting it because the stated check flags it, not because I judge the rule broken.
  - **Fix:** No stx-native replacement exists in 0.2.119 for reading the request pathname in a `<script server>` block — `useRoute()` is client-only (signals.js:1010, all getters read `window.location`). Leave as-is and add an inline comment stating this is pathname-for-active-state, not param reconstruction, so it survives the §9.10 grep in review.
- **resources/layouts/Desktop.stx:1** — [9.9] *(silent)* Full `<!DOCTYPE>` document with zero `<main>` elements — flagged by the chapter's CI check as `doc mains=0`. Leftover desktop-shell demo, not reachable from the six active app pages.
  - **Fix:** Delete the demo, or add `<main>` around the shell content. If kept, add `resources/layouts/` to the check script's skip list with a comment saying why.
- **resources/components/StartMenu.stx:52** — [9.7] *(silent)* `<button class="start-menu-footer-btn" onclick="window.location.reload()">` — inline `onclick` with raw `window.location`. Leftover desktop-shell demo. Note it would NOT be caught by `strict` even once enabled: `validateClientScript` is invoked from process.js:764 for `<script>` block contents, not for inline `onclick` attributes.
  - **Fix:** Delete the desktop demo under resources/components/ and resources/layouts/. If retained, convert to `@click` with the auto-imported `navigate(location.pathname, true)`.
- **resources/views/index.stx:94** — [10.10] Native `title=` used for hover help on five composer controls: line 94 `title="Attach image"`, line 98 `title="Schedule post"`, line 101 `title="Add post"`, line 104 `title="Preview post"`, line 71 and line 521 `title="Remove post"` (the latter inside the postTemplate string). Each already carries the correct `aria-label`, so only the tooltip mechanism is wrong.
  - **Fix:** Swap `title="…"` for `x-tooltip="…"` on all six, keeping `aria-label` intact — x-tooltip is not exposed to assistive tech. The runtime is already in the signals bundle with no registration required, and x-tooltip is in X_HANDLED (signals.js:1983) so it is not passed through as a stray DOM attribute.
- **resources/views/layouts/postline.stx:272** — [10.10] `title="Expand sidebar"` on the sidebar-expand FAB, alongside an existing `aria-label="Expand sidebar"`.
  - **Fix:** Replace with `x-tooltip="Expand sidebar"`.
- **resources/views/queue.stx:104** — [10.10] `title="Schedule a post on this day"` on each calendar day anchor. This one has no aria-label, so the title is currently doing double duty as both tooltip and (weak) accessible name.
  - **Fix:** Replace with `x-tooltip="Schedule a post on this day"` AND add an explicit `:aria-label` — x-tooltip is not exposed to assistive tech, so dropping title without adding aria-label would remove the accessible name.
- **resources/views/timeline.stx:70** — [10.10] `title="Open on Bluesky"` on the open-post anchor, which already has `aria-label="Open post on Bluesky"`.
  - **Fix:** Replace with `x-tooltip="Open on Bluesky"`.
- **resources/components/InputGroup.stx:2** — [10.12] *(silent)* `import { defineProps, withDefaults } from 'stx'` — `node_modules/stx` does not exist in this project (dependencies are `stacks` and, in dev, `better-dx`). Identical dead import on line 2 of all 13 files in resources/components/ (ContextMenu, ContextMenuItem, DesktopIcon, FeatureButton, InputGroup, LoginForm, LoginScreen, NotificationPopup, PackageItem, StartMenu, StartMenuItem, Taskbar, Window) plus resources/layouts/Desktop.stx:2. Every one of those symbols is already injected into component scope by client-script.js and destructured from window.stx at utils.js:603.
  - **Fix:** Delete the files (see 10.13). For any component that is kept, delete the import line — `grep -rn "^import" resources/components/*.stx` must return 0.
- **resources/components/InputGroup.stx:28** — [10.12] *(silent)* `class="{{ }} classList"` — an empty interpolation followed by the literal text `classList`. Verbatim the chapter's WRONG example. Renders a class attribute of `" classList"`.
  - **Fix:** Delete the file (unreferenced; TextInput/PasswordInput cover this once §10.1 lands). If kept: `class="{{ classList }}"`.
- **resources/components/FeatureButton.stx:20** — [10.12] *(silent)* `class="(active)active@endif @if feature-btn"` — an `@if` directive mangled into an attribute value with its clauses reversed. Line 21 has the same problem: `@if (onclick)onclick="{{ onclick }}"@endif` inside the tag. Verbatim the chapter's WRONG example.
  - **Fix:** Delete the file (unreferenced). If kept, compute the class in `<script server>` and interpolate a single expression, and use `@click` rather than an `onclick` string attribute.
- **resources/components/ContextMenu.stx:1** — [10.13] *(silent)* All 13 files under resources/components/ are unreferenced — the chapter's CI reference audit reports `UNREFERENCED` for every one (ContextMenu, ContextMenuItem, DesktopIcon, FeatureButton, InputGroup, LoginForm, LoginScreen, NotificationPopup, PackageItem, StartMenu, StartMenuItem, Taskbar, Window). This is worse than bughq, where at least AutofixPanel was live. They are Windows-desktop-demo scaffolding from the project template, and several shadow shipped equivalents that will resolve once §10.1 lands: NotificationPopup vs Notification, InputGroup vs TextInput, LoginForm/LoginScreen vs auth/Login. Nine of them bind `onclick="…"` to globals that do not exist (LoginForm.stx:27,29; LoginScreen.stx:24; NotificationPopup.stx:24,41; StartMenu.stx:48; Taskbar.stx:19,40). None are covered by Crosswind's `content` globs in config/crosswind.ts, so their classes generate no CSS either.
  - **Fix:** `git rm` all 13. resources/components/ then becomes the correct home for PostlineSidebar.stx (moved per the critical finding above) and any future local component.
- **resources/layouts/Desktop.stx:20** — [10.13] *(silent)* Unreferenced desktop-demo layout that contains `<!DOCTYPE html>` (line 20) and imports from the nonexistent `stx` package (line 2). resources/emails/subscription-confirmation.stx:10 also contains `<!DOCTYPE html>`, which is legitimate for an email template but should be noted as the deliberate exception.
  - **Fix:** `git rm resources/layouts/Desktop.stx`. Leave the email template as-is — the no-DOCTYPE rule in §10.12 applies to components, not to email documents.
- **resources/views/accounts.stx:96** — [10.6] Evidence of the divergence §10.6 exists to prevent, expressed in Crosswind utilities rather than `.btn` CSS (so it does NOT breach the rule as literally stated). The Mastodon Connect button is `px-4 h-9 … rounded-lg` while the structurally identical primary buttons at accounts.stx:80, accounts.stx:108, timeline.stx:16 and queue.stx:23 are `h-9 … rounded-xl`. Across the six active pages there are 27 distinct button class strings and 5 distinct badge/pill strings (`px-2.5 py-1 font-bold text-[11px] … rounded-full` ×6 vs `px-2 py-1 font-bold text-[11px] … rounded-full` ×2 vs `inline-flex … px-2.5 h-6 font-medium text-[11px] … rounded-full` ×2), so the same concept renders at three sizes.
  - **Fix:** After §10.1, replace with `<Button variant="primary" size="sm" :loading="busy()">Connect</Button>` and `<Badge variant="success" size="sm" dot>{{ status }}</Badge>`. Badge.stx also sets `role="status"` on the root, which none of Postline's pill spans have.
- **resources/views/index.stx:1062** — [10.6a] `if (publishLabel) publishLabel.textContent = 'Publishing…'` — a hand-rolled loading label written imperatively into the DOM, with the disabled state applied separately at line 1061 via `buttons.forEach(button => button.setAttribute('disabled', 'disabled'))`. This is the pattern §10.6's second CHECK targets, and it also breaches CLAUDE.md's no-vanilla-JS rule.
  - **Fix:** `<Button :loading="publishing()" @click="publish">Publish</Button>` — Button.stx swaps in a spinner, sets aria-busy, and its `isDisabled = disabled || loading` guard prevents the double-submit that the manual setAttribute is compensating for.
- **config/ui.ts:17** — [10.3b] *(silent)* No `debug` key in the default export, so component-renderer.js:699-703 never emits `[stx] <file>: unresolved component tag(s) after render passes: …`. Enabling it would have surfaced the PostlineSidebar failure at development time instead of letting it reach dist/.
  - **Fix:** Add `debug: process.env.NODE_ENV !== 'production',` to the default export in config/ui.ts.
- **resources/emails/subscription-confirmation.stx:10** — [12.6b] *(silent)* Carries `<!DOCTYPE html>` with no `@nolayout`. `grep -rn '@nolayout' resources/` returns zero hits across the whole project. The 12.9 gate script exempts resources/emails/ so this does not fail the gate, but 12.6's prose names this exact file as the case that should declare its intent explicitly instead of relying on the DOCTYPE side effect at dist/process.js:370-371.
  - **Fix:** Add `@nolayout` at the top of the file. Same for resources/views/layouts/postline.stx:1, resources/views/layouts/marketing.stx:1 and resources/layouts/Desktop.stx:20 if you want the chapter's CHECK glob to come back clean — though for those three the DOCTYPE is correct (they are layouts, which the rule text says own the document) and the gate at dist/process.js:369 is never reached for them because every Postline view arrives via @extends.
- **storage/framework/defaults/ai/skills/stacks-technical-diagrams/assets/template.html:693** — [12.2b] `bunx --bun pickier . --max-warnings 0` exits 1 across the whole tree. The single warning is pickier/sort-tailwind-classes on `class="amber card-dot"` in a vendored framework-default asset. Postline's own source is clean: `pickier resources config app --max-warnings 0` scans 175 files and exits 0 with zero warnings, and a control test confirmed pickier 0.1.44 does emit sort-tailwind-classes on .stx files, so that zero is real and not a false green.
  - **Fix:** CLAUDE.md marks storage/framework/ read-only reference, so do not edit it — add `'**/storage/framework/defaults/ai/skills/**'` to the `ignores` array in config/code-style.ts (which already ignores several storage/framework subtrees), then the chapter's literal command exits 0.
- **resources/views/welcome.stx:11** — [12.9-g3] Two internal anchors with no routing attribute: `<a … href="/welcome">` at :11 (nav logo) and :209 (footer logo). The other 10 hits the grep gate reports all carry `data-stx-link`, which the crude regex cannot see — settings.stx:44, queue.stx:23, welcome.stx:17/:37/:196/:215, index.stx:28/:33/:252, timeline.stx:15. Postline uses zero `<StxLink>` elements, so `data-stx-link` on a plain anchor is its routing convention.
  - **Fix:** Add `data-stx-link` to welcome.stx:11 and :209 so both self-links route through the router like their siblings. If a `<a href="/welcome">` is intentionally native, mark it explicitly rather than by omission. Note the gate's regex needs a `data-stx-link` exclusion before it can be a useful gate for Postline at all — as written it would report 12 forever.
- **resources/partials/welcome-pdf-content.stx:25** — [12.5] *(silent)* Six `<!-- -->` comments (:25, :32, :48, :56, :66, :80) in a leftover demo partial. Prohibited by 12.5's scope (resources/partials/), but this file is part of the desktop-shell demo, not an active app page.
  - **Fix:** Convert to `{{-- … --}}` if the demo is kept; delete the file if the desktop-shell demo is being retired.
- **resources/partials/welcome-pdf-toolbar.stx:1** — [12.9-g2] Leftover desktop-shell demo: 9 inline style attributes (:1-6 and following) plus `onclick="printWelcome()"` / `onclick="downloadWelcome()"` inline handlers at :2-3. welcome-pdf-content.stx carries 30 more inline styles. Together with resources/components/{ContextMenu:15, StartMenu:21, NotificationPopup:21+:46, Window:30, DesktopIcon:39} these account for 45 of the 76 inline-style hits, and none is on an active app page.
  - **Fix:** These are the leftover desktop-shell demo the project no longer uses. Deleting resources/components/*.stx (13 files), resources/layouts/Desktop.stx, and resources/partials/welcome-pdf-*.stx removes 45 inline styles, 6 HTML comments, one DOCTYPE and 3 of the 6 a11y findings in a single commit. If they are kept as reference, convert to Crosswind classes and @click handlers.
- **resources/views/index.stx:463** — [11.4] *(silent)* `composeTitle?.classList.toggle('block', wantsTitle)` on the blog-title <input> (resources/views/index.stx:63). `block` appears in no class= attribute in index.stx, PostlineSidebar.stx or layouts/postline.stx, so no `.block` rule is generated. When a blog target is selected the input un-hides but stays display:inline-block; it carries w-full so the visible damage is small, which is exactly why nobody noticed.
  - **Fix:** Add `block` to `safelist`, or drop the toggle entirely — removing `hidden` is sufficient here.
- **resources/views/layouts/postline.stx:307** — [11.4] *(silent)* `sidebar?.classList.remove('opacity-0')`. `opacity-0` is never added to the sidebar anywhere — not in components/PostlineSidebar.stx:67's class list, not from script — and Crosswind never generates the rule. Dead code masquerading as a fade-in.
  - **Fix:** Delete the line, or if a fade-in is intended, put `opacity-0` in the sidebar's static markup (which also makes Crosswind generate it) and remove it after mount.
- **resources/components/Window.stx:2** — [11.12] *(silent)* `import { defineProps, withDefaults } from 'stx'` — 14 occurrences, all at line 2: resources/components/{Window,Taskbar,LoginForm,LoginScreen,ContextMenu,ContextMenuItem,StartMenu,StartMenuItem,InputGroup,NotificationPopup,PackageItem,FeatureButton,DesktopIcon}.stx and resources/layouts/Desktop.stx. I verified empirically against the installed stx 0.2.119 that this is a HARD silent failure in a <script server> block, worse than the chapter's 'at best a no-op': running extractVariables on resources/components/FeatureButton.stx's server block yields `{}` with the import present and `{label:"Hi", active:false}` with it removed, and nothing is logged. variable-extractor.js:557-573 rewrites the statement to `await import('stx')`, and no `stx` package exists in node_modules (only @stacksjs/stx). Every prop in all 14 files is unbound.
  - **Fix:** Delete all 14 lines. defineProps and withDefaults are ambient in server blocks — resources/views/components/FeatureCard.stx and components/PostlineSidebar.stx already work without the import. Severity is low only because this whole tree is the leftover desktop-shell demo and is referenced by nothing (grep for <Window/<Taskbar/<StartMenu/Desktop across resources/views and components returns zero hits); if any of it is ever mounted the failure is total.
- **config/crosswind.ts:15** — [11.3b] *(silent)* `content: ['./resources/views/**/*.stx', './components/**/*.stx', './resources/assets/scripts/**/*.{ts,js}']` (lines 15-19) is dead. dev-server/crosswind.js:307 overwrites it with `content: []` on every render, in the dev path (process.js:317-318) and the production path (production-builder.js:96) alike. It is also wrong on its own terms — it omits resources/components/, resources/layouts/, resources/partials/ and resources/emails/ — and its inclusion of resources/assets/scripts/ encodes the false belief that classes in those files get scanned.
  - **Fix:** Delete the `content` key. Because it is a valid key of CrosswindConfig, `satisfies CrosswindOptions` will never flag it — the only defence is knowing the render path discards it.
- **resources/views/layouts/marketing.stx:20** — [11.7] Google Fonts loaded as an external render-blocking stylesheet (`<link href="https://fonts.googleapis.com/css2?family=Geist...">`), with the same pattern at resources/layouts/Desktop.stx:28. The chapter's table says web fonts belong in the typed config.
  - **Fix:** `fonts: { google: ['Geist:wght@300;400;500;600;700', 'Geist Mono:wght@400;500'], display: 'swap' }` in config/crosswind.ts — @cwcss/crosswind 0.2.12 exposes `fonts?: FontConfig` (types.d.ts:36, FontConfig at :22-26) and emits the @import/@font-face for you, inside the one preserved style tag.
- **resources/layouts/Desktop.stx:29** — [11.7] `<link rel="stylesheet" href="{{ stylesheetPath }}">` defaulting to /assets/styles/main.css (resources/layouts/Desktop.stx:14) — 1,532 lines of bespoke hand-written CSS, the largest single stylesheet in the repo.
  - **Fix:** This layout and its 13 components are unreferenced dead code (nothing in resources/views/ or components/ mounts them). Delete resources/layouts/Desktop.stx, resources/components/*.stx and resources/assets/styles/main.css together rather than porting 1,532 lines to shortcuts.
- **resources/components/Window.stx:30** — [11.6] *(silent)* Analogue of 11.6, not the letter of it: the chapter's comm check passes (no component in resources/components/ or components/ references any var(--x)). But these components style themselves entirely through bespoke class names — .window, .start-menu, .context-menu, .notification-popup, .taskbar — that are defined only in resources/assets/styles/main.css, which is linked only by resources/layouts/Desktop.stx:29. Mount any of them under layouts/postline.stx and they render completely unstyled, silently, for exactly the reason the chapter gives.
  - **Fix:** Delete the demo tree (see above). If any component is kept, rewrite it with utilities so it is self-sufficient in any host.
- **resources/views/components/FeatureCard.stx:1** — [11.11] *(silent)* The server block declares `const title = ''`, `const description = ''`, `const image = ''` as stand-ins for props and never calls defineProps, so the component can only ever render empty strings. It also styles itself with .area-card / .area-icon, classes that exist nowhere in the repo.
  - **Fix:** `const { title, description, image } = defineProps<FeatureCardProps>()` with the interface in types/props.ts — or delete the file, since nothing references FeatureCard.
- **tsconfig.json:16** — [11.12c] *(silent)* No types/globals.d.ts exists. The stx ambients currently reach resources/functions/*.ts only via storage/framework/tsconfig.app.json's own `include`, which the project tsconfig.json overrides — so under `bun run typecheck` neither resources/functions/dark.ts nor counter.ts is in the program at all and their globals are never verified.
  - **Fix:** Create types/globals.d.ts with `/// <reference types="@stacksjs/stx" />`, `/// <reference path="../.stx/route-types.d.ts" />` and `/// <reference path="../storage/framework/auto-imports/globals.d.ts" />`, and add types/ to `include`. `exclude` does not block files reached by reference, so the storage/framework/** exclusion at tsconfig.json:29 is not an obstacle.
- **resources/views/partials/theme.stx:5** — [11.8] The theme writer is duplicated four times — resources/views/layouts/postline.stx:52-53, resources/views/login.stx:16-17, resources/views/settings.stx:123, resources/views/partials/theme.stx:5 — and the fourth copy is dead (nothing @includes it) AND inconsistent: it defaults to 'light' where the live copies default to 'system', and it sets data-theme without ever consulting prefers-color-scheme. Rule 11.8 itself passes (all live copies set both data-theme and the dark class), but the chapter is explicit that this belongs in one place.
  - **Fix:** Delete resources/views/partials/theme.stx and hoist the remaining logic into a single hoisted module (the chapter's resources/stores/theme.ts) that both layouts inline.
